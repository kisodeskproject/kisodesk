// texto-015.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text015: PracticeText = {
  id: 'tr-text-015',
  language: LanguageCode.tr,
  content:
    'Troya antik kenti, Çanakkale yakınlarında yer alır. Farklı dönemlere ait yerleşim katmanları, bölgenin binlerce yıllık tarihini gösterir.',
};
