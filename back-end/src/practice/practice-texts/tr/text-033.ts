// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'tr-text-033',
  language: LanguageCode.tr,
  content:
    'Sayılar yazıyla yazıldığında ayrı yazılır. Yüz yirmi üç ve iki bin on beş biçimleri doğrudur.',
};
