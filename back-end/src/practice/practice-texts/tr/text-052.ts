// texto-052.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text052: PracticeText = {
  id: 'tr-text-052',
  language: LanguageCode.tr,
  content:
    'Türkiye seksen bir ilden ve dokuz yüzün üzerinde ilçeden oluşur. İdari birimlerin sayısı zamanla değişebilir.',
};
