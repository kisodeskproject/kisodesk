// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'hu-text-039',
  language: LanguageCode.hu,
  content:
    'A magyar idézőjel alsó és felső párból áll. Nyomtatásban a kezdő idézőjel lent, a záró idézőjel fent helyezkedik el: „Ez egy idézet.”',
};
