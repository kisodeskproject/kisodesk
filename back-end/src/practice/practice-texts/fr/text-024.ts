// text-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'fr-text-024',
  language: LanguageCode.fr,
  content:
    'Le h aspiré empêche généralement l\'élision et la liaison, même s\'il ne se prononce pas comme une consonne. On dit le héros et les héros sans lier les deux mots, tandis que l\'homme commence par un h muet.',
};
