// text-030.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text030: PracticeText = {
  id: 'fr-text-030',
  language: LanguageCode.fr,
  content:
    'A sans accent est une forme du verbe avoir, tandis que à avec un accent grave est une préposition. On peut écrire Elle a un clavier et Elle s\'entraîne à écrire plus vite.',
};
