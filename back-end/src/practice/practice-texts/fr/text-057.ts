// text-057.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text057: PracticeText = {
  id: 'fr-text-057',
  language: LanguageCode.fr,
  content:
    'Une école commande 15 boîtes contenant chacune 24 stylos. Elle reçoit 360 stylos. Après en avoir distribué 287, elle conserve une réserve de 73 stylos.',
};
