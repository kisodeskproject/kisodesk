// text-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'fr-text-028',
  language: LanguageCode.fr,
  content:
    'Beaucoup de mots terminés par al forment leur pluriel en aux. Un journal devient des journaux et un animal devient des animaux. Toutefois, des mots comme bal, carnaval et festival prennent un s.',
};
