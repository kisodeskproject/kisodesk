// text-053.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text053: PracticeText = {
  id: 'fr-text-053',
  language: LanguageCode.fr,
  content:
    'Un coureur effectue 5 tours de 400 m, puis 3 tours de 200 m. Il parcourt 2 600 m au total, soit 2,6 km.',
};
