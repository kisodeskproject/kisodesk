// text-031.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text031: PracticeText = {
  id: 'fr-text-031',
  language: LanguageCode.fr,
  content:
    'Et relie des mots ou des groupes de mots, alors que est appartient au verbe être. Dans la phrase Le texte est clair et précis, les deux formes ont des fonctions différentes.',
};
