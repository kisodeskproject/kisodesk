// text-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'fr-text-033',
  language: LanguageCode.fr,
  content:
    'Son peut être un déterminant possessif ou un nom lié au bruit, tandis que sont est une forme du verbe être. Son clavier produit un son discret, et ses touches sont silencieuses.',
};
