// text-092.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text092: PracticeText = {
  id: 'fr-text-092',
  language: LanguageCode.fr,
  content:
    'Babar, le célèbre éléphant en costume vert, apparut dans les albums de Jean de Brunhoff au début des années 1930. Le personnage est ensuite devenu une figure durable de la littérature et de l\'animation pour enfants.',
};
