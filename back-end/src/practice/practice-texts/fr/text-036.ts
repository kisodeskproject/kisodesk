// text-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'fr-text-036',
  language: LanguageCode.fr,
  content:
    'Ces désigne plusieurs éléments, tandis que ses indique la possession. Ces exercices sont nouveaux, mais Léa connaît déjà ses objectifs et ses difficultés.',
};
