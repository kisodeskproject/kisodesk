// text-040.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text040: PracticeText = {
  id: 'fr-text-040',
  language: LanguageCode.fr,
  content:
    'En typographie française, une espace précède généralement le point-virgule, les deux-points, le point d\'interrogation et le point d\'exclamation. Elle est idéalement insécable afin d\'éviter que le signe passe seul à la ligne.',
};
