// text-065.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text065: PracticeText = {
  id: 'fr-text-065',
  language: LanguageCode.fr,
  content:
    'En 987, Hugues Capet devint roi. Son accession au trône marque le début de la dynastie capétienne, dont les différentes branches gouvernèrent le royaume de France pendant plusieurs siècles.',
};
