// text-094.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text094: PracticeText = {
  id: 'fr-text-094',
  language: LanguageCode.fr,
  content:
    'La série Lupin reprend librement l\'univers du gentleman cambrioleur créé par Maurice Leblanc. Elle mélange enquête, vengeance, déguisements et références littéraires dans un Paris contemporain.',
};
