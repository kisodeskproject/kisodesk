// text-055.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text055: PracticeText = {
  id: 'fr-text-055',
  language: LanguageCode.fr,
  content:
    'Une entreprise reçoit 2 400 messages en 30 jours. Si le volume reste régulier, cela représente une moyenne de 80 messages par jour et environ 10 messages par heure sur une journée de 8 heures.',
};
