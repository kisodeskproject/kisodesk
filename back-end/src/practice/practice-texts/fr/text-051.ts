// text-051.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text051: PracticeText = {
  id: 'fr-text-051',
  language: LanguageCode.fr,
  content:
    'Un réservoir contient 1 200 L d\'eau. On utilise 375 L pour l\'arrosage et 225 L pour le nettoyage. Il reste exactement 600 L dans le réservoir.',
};
