// text-026.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text026: PracticeText = {
  id: 'fr-text-026',
  language: LanguageCode.fr,
  content:
    'Le pluriel de nombreux noms et adjectifs se forme en ajoutant un s qui reste souvent muet à l\'oral. Une table devient des tables, un livre devient des livres et une phrase courte devient des phrases courtes.',
};
