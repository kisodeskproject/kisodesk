// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'sv-text-039',
  language: LanguageCode.sv,
  content:
    'Sammansatta ord skrivs ihop. Särskrivningar kan ändra betydelsen, som i sjuksköterska jämfört med sjuk sköterska.',
};
