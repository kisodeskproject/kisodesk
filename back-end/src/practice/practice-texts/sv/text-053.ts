// texto-053.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text053: PracticeText = {
  id: 'sv-text-053',
  language: LanguageCode.sv,
  content:
    'Kebnekaises höjd förändras något eftersom den högsta toppen består av en glaciär. Mätningar genomförs regelbundet för att följa förändringen.',
};
