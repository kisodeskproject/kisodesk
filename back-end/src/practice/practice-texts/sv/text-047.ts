// texto-047.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'sv-text-047',
  language: LanguageCode.sv,
  content:
    'En elev får betygen 7, 8, 8, 9 och 10 på fem prov. Medelvärdet av resultaten är 8,4.',
};
