// texto-090.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text090: PracticeText = {
  id: 'sv-text-090',
  language: LanguageCode.sv,
  content:
    'Max Martin är en svensk låtskrivare och producent som har arbetat med många internationella popartister. Flera av hans låtar har toppat stora listor.',
};
