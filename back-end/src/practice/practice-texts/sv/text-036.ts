// texto-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'sv-text-036',
  language: LanguageCode.sv,
  content:
    'Ordföljden i en huvudsats följer ofta regeln att verbet står på andra plats. Därför skriver man i dag arbetar jag hemma.',
};
