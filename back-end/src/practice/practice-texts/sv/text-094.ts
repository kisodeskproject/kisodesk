// texto-094.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text094: PracticeText = {
  id: 'sv-text-094',
  language: LanguageCode.sv,
  content:
    'Nobelpriset delas ut i Stockholm den 10 december, med undantag för fredspriset som delas ut i Oslo.',
};
