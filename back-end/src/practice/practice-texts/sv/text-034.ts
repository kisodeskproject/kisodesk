// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'sv-text-034',
  language: LanguageCode.sv,
  content:
    'Preteritum av svaga verb slutar ofta på -de, -te eller -dde. Exempel är spelade, köpte och bodde.',
};
