// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'sv-text-033',
  language: LanguageCode.sv,
  content:
    'Presens bildas ofta med ändelsen -r eller -er. Exempel är talar, läser, skriver och bor.',
};
