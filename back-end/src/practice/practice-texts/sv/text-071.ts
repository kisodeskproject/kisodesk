// texto-071.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text071: PracticeText = {
  id: 'sv-text-071',
  language: LanguageCode.sv,
  content:
    'Det stora nordiska kriget pågick mellan 1700 och 1721. Efter kriget minskade Sveriges ställning som europeisk stormakt.',
};
