// texto-045.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text045: PracticeText = {
  id: 'sv-text-045',
  language: LanguageCode.sv,
  content:
    'Ett svenskt postnummer består av fem siffror, ofta skrivna med ett mellanrum efter de tre första, till exempel 123 45.',
};
