// texto-054.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text054: PracticeText = {
  id: 'sv-text-054',
  language: LanguageCode.sv,
  content:
    'Vänern har en yta på mer än 5500 kvadratkilometer. Sjön innehåller många öar och omges av flera landskap.',
};
