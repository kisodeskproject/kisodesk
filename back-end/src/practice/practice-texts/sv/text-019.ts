// texto-019.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text019: PracticeText = {
  id: 'sv-text-019',
  language: LanguageCode.sv,
  content:
    'Sverige är känt för många uppfinningar och innovationer. Dynamit, säkerhetständstickan och trepunktsbältet har stark koppling till svenska uppfinnare.',
};
