// texto-041.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text041: PracticeText = {
  id: 'sv-text-041',
  language: LanguageCode.sv,
  content:
    'Tåget avgår klockan 08.35 och anländer klockan 11.10. Resan tar 2 timmar och 35 minuter.',
};
