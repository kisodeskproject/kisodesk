// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'sv-text-035',
  language: LanguageCode.sv,
  content:
    'Perfekt bildas med har och supinum. Man skriver jag har läst, hon har arbetat och de har kommit.',
};
