// texto-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'da-text-036',
  language: LanguageCode.da,
  content:
    'Komma kan sættes efter startkomma eller uden startkomma, men man skal følge samme system konsekvent i en tekst.',
};
