// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'da-text-028',
  language: LanguageCode.da,
  content:
    'Nutid ender ofte på -r. Eksempler er taler, læser, skriver og bor.',
};
