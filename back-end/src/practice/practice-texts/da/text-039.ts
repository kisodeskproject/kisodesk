// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'da-text-039',
  language: LanguageCode.da,
  content:
    'I helsætninger står det bøjede udsagnsord normalt på anden plads. Derfor hedder det i dag arbejder jeg hjemme.',
};
