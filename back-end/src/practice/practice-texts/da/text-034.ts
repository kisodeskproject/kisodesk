// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'da-text-034',
  language: LanguageCode.da,
  content:
    'Bogstavet d kan være stumt eller blødt. Det høres forskelligt i ord som dag, mad, hånd og gade.',
};
