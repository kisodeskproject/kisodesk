// texto-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'da-text-024',
  language: LanguageCode.da,
  content:
    'Bestemt form dannes ofte med en endelse. En bog bliver bogen, og et hus bliver huset.',
};
