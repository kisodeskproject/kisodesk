// texto-029.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'da-text-029',
  language: LanguageCode.da,
  content:
    'Datid af svage udsagnsord ender ofte på -ede, -te eller -de. Eksempler er arbejdede, købte og levede.',
};
