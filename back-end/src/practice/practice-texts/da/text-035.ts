// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'da-text-035',
  language: LanguageCode.da,
  content:
    'Det bløde d har ingen helt direkte parallel i mange andre sprog. Det optræder blandt andet i mad, gade og ude.',
};
