// texto-016.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text016: PracticeText = {
  id: 'pt-text-016',
  language: LanguageCode.pt,
  content:
    'A calçada portuguesa é construída com pequenas pedras claras e escuras, geralmente de calcário e basalto. Os calceteiros dispõem as pedras à mão para formar ondas, figuras geométricas, símbolos e desenhos figurativos.',
};
