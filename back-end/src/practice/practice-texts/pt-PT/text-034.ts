// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'pt-text-034',
  language: LanguageCode.pt,
  content:
    'O som de S pode ser representado por S, SS, C, Ç, X ou Z. Compare-se casa, passo, cidade, açúcar, próximo e feliz; a escolha depende da origem e da estrutura de cada palavra.',
};
