// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'pt-text-035',
  language: LanguageCode.pt,
  content:
    'As letras G e J podem representar sons semelhantes antes de E ou I. Escrevem-se gelo, girafa, jeito e viagem, por isso a leitura não basta sempre para decidir qual letra deve ser usada.',
};
