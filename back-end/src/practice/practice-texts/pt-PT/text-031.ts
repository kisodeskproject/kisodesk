// texto-031.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text031: PracticeText = {
  id: 'pt-text-031',
  language: LanguageCode.pt,
  content:
    'O Acordo Ortográfico eliminou consoantes que não são pronunciadas em certas palavras. Por isso, escreve-se ação, ótimo e diretor; porém, mantém-se a consoante em palavras como facto e contacto quando ela é efetivamente pronunciada.',
};
