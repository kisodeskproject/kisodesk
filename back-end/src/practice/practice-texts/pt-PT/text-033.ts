// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'pt-text-033',
  language: LanguageCode.pt,
  content:
    'Antes de P e B, costuma escrever-se M em vez de N. Esta regra aparece em palavras como campo, tempo, também, sombra e ambiente, embora existam compostos e nomes próprios que exigem atenção especial.',
};
