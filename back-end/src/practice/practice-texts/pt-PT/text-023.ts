// texto-023.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text023: PracticeText = {
  id: 'pt-text-023',
  language: LanguageCode.pt,
  content:
    'O til indica nasalidade e aparece sobre as vogais A e O. Palavras como manhã, limões, põe e informações exigem atenção porque o sinal altera tanto a pronúncia como a escrita.',
};
