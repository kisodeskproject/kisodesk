// texto-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'pt-text-036',
  language: LanguageCode.pt,
  content:
    'O plural das palavras terminadas em -ão não segue uma única regra. Coração forma corações, cidadão forma cidadãos e pão forma pães, sendo necessário aprender o plural juntamente com cada palavra.',
};
