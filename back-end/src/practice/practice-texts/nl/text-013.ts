// texto-013.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text013: PracticeText = {
  id: 'nl-text-013',
  language: LanguageCode.nl,
  content:
    'Nederland heeft een dicht spoorwegnet. Veel reizigers combineren trein, bus, tram, metro en fiets tijdens één reis.',
};
