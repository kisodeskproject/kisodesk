// texto-055.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text055: PracticeText = {
  id: 'nl-text-055',
  language: LanguageCode.nl,
  content:
    'Koningsdag wordt gevierd op 27 april. Als die datum op een zondag valt, wordt de viering meestal op 26 april gehouden.',
};
