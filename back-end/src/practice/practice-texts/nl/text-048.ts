// texto-048.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text048: PracticeText = {
  id: 'nl-text-048',
  language: LanguageCode.nl,
  content:
    'Een watertank bevat 80 liter en verliest 15 procent van de inhoud. Daarna blijft 68 liter over.',
};
