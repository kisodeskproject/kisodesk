// texto-011.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text011: PracticeText = {
  id: 'nl-text-011',
  language: LanguageCode.nl,
  content:
    'Giethoorn is bekend om grachten, bruggetjes en huizen met rieten daken. In het historische centrum worden veel korte afstanden per boot afgelegd.',
};
