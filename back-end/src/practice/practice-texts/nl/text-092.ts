// texto-092.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text092: PracticeText = {
  id: 'nl-text-092',
  language: LanguageCode.nl,
  content:
    'Tiesto bouwde een internationale carrière op als dj. Hij trad op bij grote festivals, sportevenementen en concertzalen.',
};
