// texto-080.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text080: PracticeText = {
  id: 'nl-text-080',
  language: LanguageCode.nl,
  content:
    'De Watersnoodramp van 1953 leidde tot de bouw van de Deltawerken. Deze grote waterbouwkundige projecten moesten een nieuwe ramp helpen voorkomen.',
};
