// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'nl-text-035',
  language: LanguageCode.nl,
  content:
    'De woorden de en het zijn bepaalde lidwoorden. De hoort bij de meeste zelfstandige naamwoorden, terwijl het bij een kleinere groep hoort.',
};
