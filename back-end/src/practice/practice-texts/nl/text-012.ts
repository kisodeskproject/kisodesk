// texto-012.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text012: PracticeText = {
  id: 'nl-text-012',
  language: LanguageCode.nl,
  content:
    'De Hoge Veluwe is een nationaal park met bossen, heidevelden, zandverstuivingen en graslanden. Bezoekers kunnen er wandelen, fietsen en wilde dieren observeren.',
};
