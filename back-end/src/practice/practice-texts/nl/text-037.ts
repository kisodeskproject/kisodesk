// texto-037.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'nl-text-037',
  language: LanguageCode.nl,
  content:
    'Na een dubbele punt kan een opsomming, verklaring of letterlijk citaat volgen. Een hoofdletter is alleen nodig als daarna een volledige geciteerde zin begint.',
};
