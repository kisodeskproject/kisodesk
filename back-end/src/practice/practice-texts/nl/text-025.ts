// texto-025.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'nl-text-025',
  language: LanguageCode.nl,
  content:
    'In een open lettergreep is één klinker vaak voldoende om een lange klank aan te geven. Men schrijft ramen, benen, bomen en muren.',
};
