// texto-022.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text022: PracticeText = {
  id: 'nl-text-022',
  language: LanguageCode.nl,
  content:
    'De lettercombinatie ij wordt in het Nederlands als één klank uitgesproken. In woorden als ijs, tijd en blijven staan de letters altijd samen.',
};
