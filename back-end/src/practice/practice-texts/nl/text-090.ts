// texto-090.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text090: PracticeText = {
  id: 'nl-text-090',
  language: LanguageCode.nl,
  content:
    'Doe Maar combineerde pop, reggae en Nederlandstalige teksten. De band was bijzonder populair in de jaren tachtig.',
};
