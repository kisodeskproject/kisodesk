// texto-020.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text020: PracticeText = {
  id: 'en-text-020',
  language: LanguageCode.en,
  content:
    'Many American cities have familiar nicknames. Chicago is often called the Windy City, New York is known as the Big Apple, and New Orleans is called the Crescent City. These names reflect history, geography, promotion, and local identity.',
};
