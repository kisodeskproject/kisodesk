// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'en-text-034',
  language: LanguageCode.en,
  content:
    'The serial comma appears before the final conjunction in a list of three or more items. American publications often use it because it can prevent ambiguity. For example: We invited the teachers, the students, and the parents.',
};
