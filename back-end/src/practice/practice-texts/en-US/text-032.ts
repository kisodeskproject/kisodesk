// texto-032.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text032: PracticeText = {
  id: 'en-text-032',
  language: LanguageCode.en,
  content:
    'Affect is usually a verb meaning to influence, while effect is usually a noun meaning a result. The weather may affect travel, and the effect may be a delay. Some specialized uses differ, but this distinction works in most everyday writing.',
};
