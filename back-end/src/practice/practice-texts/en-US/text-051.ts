// texto-051.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text051: PracticeText = {
  id: 'en-text-051',
  language: LanguageCode.en,
  content:
    'A classroom survey includes 80 students. Forty-eight prefer digital books, 20 prefer printed books, and 12 have no preference. Digital books receive 60 percent of the responses.',
};
