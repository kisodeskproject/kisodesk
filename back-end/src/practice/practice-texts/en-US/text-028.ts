// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'en-text-028',
  language: LanguageCode.en,
  content:
    'Use fewer with countable items and less with quantities that are not usually counted individually. Write fewer mistakes, fewer chairs, and fewer emails. Write less water, less traffic, and less time.',
};
