// texto-050.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text050: PracticeText = {
  id: 'en-text-050',
  language: LanguageCode.en,
  content:
    'A family budgets $3,200 for one month. Housing costs $1,450, food costs $620, transportation costs $410, and utilities cost $280. After these expenses, $440 remains for savings and other needs.',
};
