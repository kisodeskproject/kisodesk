// texto-043.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text043: PracticeText = {
  id: 'en-text-043',
  language: LanguageCode.en,
  content:
    'A concert ticket costs $48.50, and the service fee is $6.75. The subtotal is $55.25. With a sales tax of $4.42, the final charge becomes $59.67.',
};
