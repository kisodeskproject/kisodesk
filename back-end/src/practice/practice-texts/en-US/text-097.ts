// texto-097.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text097: PracticeText = {
  id: 'en-text-097',
  language: LanguageCode.en,
  content:
    'The Thanksgiving episode is a recurring feature of many American television series. Writers use the holiday meal to bring characters together, reveal family tensions, create misunderstandings, and end with reconciliation or comic disaster.',
};
