// texto-008.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text008: PracticeText = {
  id: 'en-text-008',
  language: LanguageCode.en,
  content:
    'California contains both the highest point and the lowest point in the contiguous United States. Mount Whitney rises in the Sierra Nevada, while Badwater Basin lies below sea level in Death Valley. The two places are separated by a surprisingly short distance.',
};
