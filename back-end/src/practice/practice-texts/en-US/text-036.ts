// texto-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'en-text-036',
  language: LanguageCode.en,
  content:
    'Use an em dash to mark a strong break in thought, an interruption, or an emphasized conclusion. It is longer than a hyphen. In many American styles, no spaces appear on either side of the em dash.',
};
