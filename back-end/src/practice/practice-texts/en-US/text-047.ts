// texto-047.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'en-text-047',
  language: LanguageCode.en,
  content:
    'A runner completes 6.2 miles in 54 minutes and 33 seconds. The course is approximately the length of a 10-kilometer race. Careful pacing helps the runner maintain a steady effort from start to finish.',
};
