// texto-057.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text057: PracticeText = {
  id: 'en-text-057',
  language: LanguageCode.en,
  content:
    'A train travels 360 miles in six hours at an average speed of 60 miles per hour. If the same distance took eight hours, the average speed would fall to 45 miles per hour.',
};
