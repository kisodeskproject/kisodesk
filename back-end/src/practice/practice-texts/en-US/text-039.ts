// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'en-text-039',
  language: LanguageCode.en,
  content:
    'American English commonly spells words such as color, center, traveled, and defense without the forms color, centre, travelled, and defence used in other varieties. Consistency matters most when a document follows a particular regional style.',
};
