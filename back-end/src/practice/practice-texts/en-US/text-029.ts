// texto-029.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'en-text-029',
  language: LanguageCode.en,
  content:
    'Their, there, and they\'re sound alike but serve different purposes. Their shows possession, there refers to a place or existence, and they\'re is a contraction of they are. Reading the full sentence aloud can reveal the correct choice.',
};
