// texto-084.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text084: PracticeText = {
  id: 'en-text-084',
  language: LanguageCode.en,
  content:
    'Hip-hop emerged in the Bronx during the 1970s. Its culture includes rapping, DJing, breakdancing, graffiti, fashion, and community expression. It grew from local gatherings into a global force in music, language, visual art, and business.',
};
