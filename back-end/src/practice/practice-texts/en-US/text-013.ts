// texto-013.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text013: PracticeText = {
  id: 'en-text-013',
  language: LanguageCode.en,
  content:
    'The United States has no official language at the federal level. English is the most widely used language, but communities across the country speak hundreds of others. Spanish, Chinese languages, Tagalog, Vietnamese, French, and many Indigenous languages are among them.',
};
