// texto-053.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text053: PracticeText = {
  id: 'en-text-053',
  language: LanguageCode.en,
  content:
    'A water tank holds 500 gallons when full. After 135 gallons are used, 365 gallons remain. Adding 75 gallons raises the amount to 440 gallons, or 88 percent of capacity.',
};
