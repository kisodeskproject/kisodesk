// texto-018.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text018: PracticeText = {
  id: 'en-text-018',
  language: LanguageCode.en,
  content:
    'Sequoia trees can live for thousands of years and grow to extraordinary sizes. Giant sequoias are known for massive trunks, while coast redwoods are the tallest trees on Earth. Both species depend on particular climates and careful forest management.',
};
