// texto-059.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text059: PracticeText = {
  id: 'en-text-059',
  language: LanguageCode.en,
  content:
    'A survey reports that 37.5 percent of 640 respondents chose option A. Multiplying 640 by 0.375 gives 240 people. The remaining 400 respondents selected other options.',
};
