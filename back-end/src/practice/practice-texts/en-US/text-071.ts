// texto-071.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text071: PracticeText = {
  id: 'en-text-071',
  language: LanguageCode.en,
  content:
    'The forced removal of Native nations from the Southeast intensified during the 1830s. Thousands of Cherokee people were driven west on the Trail of Tears. Many died from disease, exposure, hunger, and exhaustion.',
};
