// texto-016.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text016: PracticeText = {
  id: 'en-text-016',
  language: LanguageCode.en,
  content:
    'Arizona does not observe daylight saving time in most of the state. This means its relationship with neighboring time zones changes during the year. The Navajo Nation, however, does observe daylight saving time, creating an important regional exception.',
};
