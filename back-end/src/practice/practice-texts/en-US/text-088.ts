// texto-088.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text088: PracticeText = {
  id: 'en-text-088',
  language: LanguageCode.en,
  content:
    'American diners are associated with long counters, booths, coffee, breakfast plates, sandwiches, pies, and late-night service. Their shiny metal exteriors and neon signs became familiar images in movies, photography, and roadside culture.',
};
