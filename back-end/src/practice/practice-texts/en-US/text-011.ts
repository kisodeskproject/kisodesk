// texto-011.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text011: PracticeText = {
  id: 'en-text-011',
  language: LanguageCode.en,
  content:
    'New York City\'s subway system operates through hundreds of stations and many interconnected lines. It carries passengers at all hours and links neighborhoods across several boroughs. Riders learn to recognize letters, numbers, colors, and transfer points quickly.',
};
