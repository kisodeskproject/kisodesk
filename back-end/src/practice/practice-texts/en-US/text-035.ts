// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'en-text-035',
  language: LanguageCode.en,
  content:
    'Hyphens connect words that work together before a noun, especially when they prevent confusion. Write a well-known author or a ten-minute break. The hyphen is often removed when the same phrase follows the noun.',
};
