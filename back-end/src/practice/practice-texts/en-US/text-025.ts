// texto-025.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'en-text-025',
  language: LanguageCode.en,
  content:
    'Use a comma after an introductory word, phrase, or clause when it improves clarity. For example: After the storm ended, the streets became quiet. Short introductory elements may omit the comma when no confusion is likely.',
};
