// texto-046.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text046: PracticeText = {
  id: 'en-text-046',
  language: LanguageCode.en,
  content:
    'A library records 3,840 visits in April. Adults account for 2,150 visits, teenagers for 970, and children for 720. Together, the three groups equal the full monthly total.',
};
