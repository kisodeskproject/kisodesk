// texto-037.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'en-text-037',
  language: LanguageCode.en,
  content:
    'Singular subjects normally take singular verbs, and plural subjects take plural verbs. The dog runs, while the dogs run. Words between the subject and verb do not change the number of the true subject.',
};
