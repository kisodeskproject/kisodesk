// texto-031.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text031: PracticeText = {
  id: 'en-text-031',
  language: LanguageCode.en,
  content:
    'Its is the possessive form of it, while it\'s means it is or it has. Write “The company changed its policy” and “It\'s already noon.” Unlike most possessives, its does not contain an apostrophe.',
};
