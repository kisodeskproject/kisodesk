// texto-058.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text058: PracticeText = {
  id: 'en-text-058',
  language: LanguageCode.en,
  content:
    'A school goal is to collect 2,500 cans. During the first week, students collect 875; during the second, they collect 1,040. They still need 585 cans to reach the target.',
};
