// texto-022.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text022: PracticeText = {
  id: 'en-text-022',
  language: LanguageCode.en,
  content:
    'A complete sentence normally contains a subject and a predicate and expresses a complete thought. The subject identifies who or what the sentence concerns. The predicate explains what the subject does, has, feels, or is.',
};
