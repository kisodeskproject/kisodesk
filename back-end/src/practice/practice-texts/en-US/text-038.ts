// texto-038.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text038: PracticeText = {
  id: 'en-text-038',
  language: LanguageCode.en,
  content:
    'Pronouns should agree clearly with the nouns they replace. A pronoun must match its antecedent in number and refer to it without ambiguity. Repeating a noun is often better than leaving the reader unsure about who acted.',
};
