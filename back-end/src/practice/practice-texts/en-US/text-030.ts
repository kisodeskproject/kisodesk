// texto-030.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text030: PracticeText = {
  id: 'en-text-030',
  language: LanguageCode.en,
  content:
    'Your shows possession, while you\'re means you are. The sentence “You\'re carrying your umbrella” uses both forms correctly. Expanding the contraction is a reliable way to check which spelling belongs in the sentence.',
};
