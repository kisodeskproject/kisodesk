// texto-041.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text041: PracticeText = {
  id: 'en-text-041',
  language: LanguageCode.en,
  content:
    'A store receives 240 notebooks and packs them equally into 12 boxes. Each box contains 20 notebooks. If three boxes are shipped in the morning, 60 notebooks leave the warehouse and 180 remain.',
};
