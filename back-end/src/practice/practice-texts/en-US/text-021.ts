// texto-021.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text021: PracticeText = {
  id: 'en-text-021',
  language: LanguageCode.en,
  content:
    'In American English, proper nouns begin with capital letters. Names of people, cities, states, countries, holidays, and specific organizations are capitalized. Common nouns remain lowercase unless they begin a sentence or form part of an official title.',
};
