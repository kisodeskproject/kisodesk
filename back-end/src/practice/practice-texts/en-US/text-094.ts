// texto-094.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text094: PracticeText = {
  id: 'en-text-094',
  language: LanguageCode.en,
  content:
    'The American road movie uses travel to explore freedom, friendship, escape, conflict, and personal change. Highways, motels, gas stations, diners, and open landscapes become more than settings; they help shape the characters\' journeys.',
};
