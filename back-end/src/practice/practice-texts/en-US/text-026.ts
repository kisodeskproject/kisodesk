// texto-026.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text026: PracticeText = {
  id: 'en-text-026',
  language: LanguageCode.en,
  content:
    'A semicolon can join two closely related independent clauses without a coordinating conjunction. For example: The train arrived late; the passengers remained patient. Both sides of the semicolon must be able to stand as complete sentences.',
};
