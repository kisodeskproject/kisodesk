// texto-054.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text054: PracticeText = {
  id: 'en-text-054',
  language: LanguageCode.en,
  content:
    'A meeting begins at 1:35 p.m. and ends at 3:10 p.m. It lasts 1 hour and 35 minutes. A 15-minute break divides the working time into two shorter sessions.',
};
