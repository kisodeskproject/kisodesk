// texto-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'en-text-024',
  language: LanguageCode.en,
  content:
    'In American style, periods and commas usually appear inside closing quotation marks. For example: She said, “The meeting starts at nine.” Question marks and exclamation points depend on whether they belong to the quoted words or the entire sentence.',
};
