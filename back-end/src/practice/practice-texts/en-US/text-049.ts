// texto-049.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text049: PracticeText = {
  id: 'en-text-049',
  language: LanguageCode.en,
  content:
    'A phone battery begins at 92 percent. Streaming video uses 27 percentage points, navigation uses 18, and calls use 12. The remaining charge is 35 percent.',
};
