// texto-040.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text040: PracticeText = {
  id: 'en-text-040',
  language: LanguageCode.en,
  content:
    'Numbers at the beginning of a sentence are usually written as words. Instead of starting with 27 participants, write Twenty-seven participants. Another option is to revise the sentence so the numeral appears later.',
};
