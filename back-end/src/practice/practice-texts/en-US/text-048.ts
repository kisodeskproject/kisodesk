// texto-048.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text048: PracticeText = {
  id: 'en-text-048',
  language: LanguageCode.en,
  content:
    'A theater has 18 rows with 24 seats in each row. Multiplying 18 by 24 gives a capacity of 432 seats. If 397 tickets are sold, 35 seats remain available.',
};
