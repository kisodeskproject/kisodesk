// texto-060.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text060: PracticeText = {
  id: 'en-text-060',
  language: LanguageCode.en,
  content:
    'A calendar year usually has 365 days, or 8,760 hours. A leap year has 366 days because February gains an extra day. This adjustment keeps the calendar aligned more closely with Earth\'s orbit.',
};
