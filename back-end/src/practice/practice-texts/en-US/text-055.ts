// texto-055.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text055: PracticeText = {
  id: 'en-text-055',
  language: LanguageCode.en,
  content:
    'A laptop is advertised for $900 with a 15 percent discount. The discount equals $135, reducing the price to $765 before tax. A customer should calculate the final amount using the local sales tax rate.',
};
