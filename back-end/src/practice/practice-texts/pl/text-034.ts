// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'pl-text-034',
  language: LanguageCode.pl,
  content:
    'Przecinek stawia się przed spójnikami ale, lecz, jednak, więc i dlatego. Zwykle nie stawia się go przed pojedynczym i, lub albo.',
};
