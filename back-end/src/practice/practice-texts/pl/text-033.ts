// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'pl-text-033',
  language: LanguageCode.pl,
  content:
    'Wołacz jest używany przy bezpośrednim zwracaniu się do kogoś. Formy takie jak Anno, Piotrze i panie doktorze wymagają przecinka.',
};
