// texto-037.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'pl-text-037',
  language: LanguageCode.pl,
  content:
    'Skróty takie jak dr, mgr i nr zapisuje się bez kropki, jeśli kończą się ostatnią literą pełnego wyrazu. Inne skróty mogą wymagać kropki.',
};
