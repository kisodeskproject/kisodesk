// texto-025.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'pl-text-025',
  language: LanguageCode.pl,
  content:
    'Dwuznak CH i litera H są zwykle wymawiane tak samo. W wielu wyrazach poprawną formę należy zapamiętać, na przykład chleb, humor i historia.',
};
