// texto-056.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text056: PracticeText = {
  id: 'pl-text-056',
  language: LanguageCode.pl,
  content:
    'Narodowe Święto Niepodległości przypada 11 listopada. Data wiąże się z odzyskaniem niepodległości w 1918 roku.',
};
