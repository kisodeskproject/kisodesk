// texto-047.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'pl-text-047',
  language: LanguageCode.pl,
  content:
    'Uczeń otrzymał oceny 4, 5, 5, 5 i 6. Średnia z tych pięciu wyników wynosi 5.',
};
