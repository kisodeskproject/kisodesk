// texto-065.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text065: PracticeText = {
  id: 'pl-text-065',
  language: LanguageCode.pl,
  content:
    'Władysław Łokietek został koronowany w 1320 roku. Jego panowanie symbolizuje ponowne zjednoczenie znacznej części kraju.',
};
