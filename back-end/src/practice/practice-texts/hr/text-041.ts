// texto-041.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text041: PracticeText = {
  id: 'hr-text-041',
  language: LanguageCode.hr,
  content:
    'Vlak polazi u 8:35 i stiže u 11:12. Putovanje traje 2 sata i 37 minuta, a putnici trebaju biti na peronu najmanje 10 minuta prije polaska.',
};
