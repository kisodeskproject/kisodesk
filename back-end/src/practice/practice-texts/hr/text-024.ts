// texto-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'hr-text-024',
  language: LanguageCode.hr,
  content:
    'Glasovni skupovi ije i je ovise o razvoju staroga glasa jat. Zato se piše mlijeko, vrijeme i riječ, ali mjesto, pjesma i vjera.',
};
