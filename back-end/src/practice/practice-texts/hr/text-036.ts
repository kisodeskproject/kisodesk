// texto-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'hr-text-036',
  language: LanguageCode.hr,
  content:
    'Prijedlozi s i sa biraju se prema izgovoru. Kažemo s prijateljem, s posla, sa sestrom i sa školom.',
};
