// texto-012.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text012: PracticeText = {
  id: 'hr-text-012',
  language: LanguageCode.hr,
  content:
    'Paška čipka izrađuje se ručno tankim koncem i iglom. Složeni geometrijski uzorci prenose se generacijama i povezani su s tradicijom otoka Paga.',
};
