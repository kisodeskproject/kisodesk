// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'hr-text-033',
  language: LanguageCode.hr,
  content:
    'Ispred veznika i, pa, te, ni i niti zarez se obično ne piše kada povezuju ravnopravne dijelove rečenice.',
};
