// texto-047.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'hr-text-047',
  language: LanguageCode.hr,
  content:
    'Učenik je dobio ocjene 3, 4, 4, 5 i 5. Zbroj ocjena je 21, a prosjek iznosi 4,2.',
};
