// texto-016.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text016: PracticeText = {
  id: 'hr-text-016',
  language: LanguageCode.hr,
  content:
    'Hum na istarskom poluotoku često se predstavlja kao jedan od najmanjih gradova na svijetu. Njegove zidine, kamene kuće i gradska vrata stvaraju dojam minijaturnog povijesnog središta.',
};
