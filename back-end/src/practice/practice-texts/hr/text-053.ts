// texto-053.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text053: PracticeText = {
  id: 'hr-text-053',
  language: LanguageCode.hr,
  content:
    'Najviši vrh Hrvatske je Dinara, visoka 1831 metar. Nalazi se uz granicu s Bosnom i Hercegovinom.',
};
