// texto-067.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text067: PracticeText = {
  id: 'hr-text-067',
  language: LanguageCode.hr,
  content:
    'Hrvatsko Kraljevstvo ušlo je početkom dvanaestog stoljeća u personalnu uniju s Ugarskom. Dvije su zemlje dijelile vladara, ali su zadržale određene posebnosti.',
};
