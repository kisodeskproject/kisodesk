// texto-018.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text018: PracticeText = {
  id: 'hr-text-018',
  language: LanguageCode.hr,
  content:
    'Kornati su arhipelag s velikim brojem suhih, kamenitih otoka i otočića. Njihov krajolik oblikovali su more, vjetar, sunce i stoljetno stočarstvo.',
};
