// texto-040.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text040: PracticeText = {
  id: 'hr-text-040',
  language: LanguageCode.hr,
  content:
    'Sastavljeno ili rastavljeno pisanje riječi ovisi o njihovu značenju i tvorbi. Zato se razlikuju oblici napamet i na pamet, ubuduće i u buduće.',
};
