// texto-052.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text052: PracticeText = {
  id: 'hr-text-052',
  language: LanguageCode.hr,
  content:
    'Hrvatska ima 20 županija i Grad Zagreb, koji ima poseban položaj. Svaka županija obuhvaća gradove, općine i naselja.',
};
