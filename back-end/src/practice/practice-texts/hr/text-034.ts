// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'hr-text-034',
  language: LanguageCode.hr,
  content:
    'Izravni govor piše se navodnicima ili crticom, ovisno o načinu oblikovanja teksta. Primjer je: „Dolazim sutra“, rekla je Marija.',
};
