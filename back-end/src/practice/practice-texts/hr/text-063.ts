// texto-063.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text063: PracticeText = {
  id: 'hr-text-063',
  language: LanguageCode.hr,
  content:
    'Rimsko Carstvo organiziralo je ovo područje u više provincija. Gradovi poput Pule, Salone i Narone bili su važna upravna, vojna i trgovačka središta.',
};
