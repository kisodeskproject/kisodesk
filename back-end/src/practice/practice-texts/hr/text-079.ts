// texto-079.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text079: PracticeText = {
  id: 'hr-text-079',
  language: LanguageCode.hr,
  content:
    'Hrvatska je 1991. proglasila neovisnost. Uslijedio je rat koji je trajao više godina i ostavio teške ljudske, gospodarske i materijalne posljedice.',
};
