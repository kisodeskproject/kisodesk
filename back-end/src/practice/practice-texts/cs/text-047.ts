// texto-047.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'cs-text-047',
  language: LanguageCode.cs,
  content:
    'Student získal známky 1, 2, 2, 3 a 2. Součet známek je 10 a průměr činí 2,0.',
};
