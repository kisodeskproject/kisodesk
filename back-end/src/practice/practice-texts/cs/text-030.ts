// texto-030.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text030: PracticeText = {
  id: 'cs-text-030',
  language: LanguageCode.cs,
  content:
    'Předložky s a z se pojí s různými pády. Píšeme s kamarádem, se sestrou, z domu a ze školy.',
};
