// texto-046.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text046: PracticeText = {
  id: 'cs-text-046',
  language: LanguageCode.cs,
  content:
    'Mezinárodní telefonní předvolba Česka je +420. Mobilní číslo může být zapsáno například jako +420 601 234 567.',
};
