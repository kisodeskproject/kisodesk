// texto-099.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text099: PracticeText = {
  id: 'cs-text-099',
  language: LanguageCode.cs,
  content:
    'Festival Pražské jaro každoročně představuje orchestry, sólisty a komorní soubory. Program se zaměřuje především na klasickou hudbu.',
};
