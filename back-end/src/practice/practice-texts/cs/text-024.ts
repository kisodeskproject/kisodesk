// texto-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'cs-text-024',
  language: LanguageCode.cs,
  content:
    'Souhlásky ď, ť a ň se zapisují s háčkem. Před písmenem ě se však háček samostatně nepíše, například ve slovech děda, tělo a něco.',
};
