// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'cs-text-028',
  language: LanguageCode.cs,
  content:
    'Vyjmenovaná slova pomáhají určit, kdy se po obojetné souhlásce píše y. Ke každému základnímu slovu patří také slova příbuzná.',
};
