// texto-052.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text052: PracticeText = {
  id: 'cs-text-052',
  language: LanguageCode.cs,
  content:
    'Česko se dělí na 14 krajů, mezi nimiž má zvláštní postavení hlavní město Praha. Každý kraj se dále člení na menší správní jednotky.',
};
