// texto-037.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'pt-text-037',
  language: LanguageCode.pt,
  content:
    'Quando o prefixo termina em vogal e o segundo elemento começa com r ou s, não se usa hífen e a consoante é duplicada. Escrevem-se antirracista, minissaia, ultrassom e contrarregra.',
};
