// texto-038.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text038: PracticeText = {
  id: 'pt-text-038',
  language: LanguageCode.pt,
  content:
    'Com prefixos terminados em vogal e palavras iniciadas por vogal diferente, geralmente não se usa hífen. Autoescola, infraestrutura, semianalfabeto e extraoficial seguem esse padrão.',
};
