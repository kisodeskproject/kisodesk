// texto-029.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'pt-text-029',
  language: LanguageCode.pt,
  content:
    'Porque explica ou responde; por que aparece em perguntas ou equivale a pelo qual; por quê é usado no fim de uma pergunta; e porquê funciona como substantivo. O contexto determina a forma correta.',
};
