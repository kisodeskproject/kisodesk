// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'pt-text-039',
  language: LanguageCode.pt,
  content:
    'Emprega-se m antes de p e b, como em campo, também, sempre e sombra. Diante de outras consoantes, costuma-se usar n, como em canto, inverno, convite e ansiedade.',
};
