// texto-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'it-text-036',
  language: LanguageCode.it,
  content:
    'Perché può introdurre una domanda, una causa o una spiegazione. In italiano standard si scrive con l\'accento acuto sulla E finale.',
};
