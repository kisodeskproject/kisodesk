// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'it-text-034',
  language: LanguageCode.it,
  content:
    'Qual è si scrive senza apostrofo. La forma qual deriva da un troncamento e non da un\'elisione.',
};
