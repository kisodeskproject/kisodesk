// texto-026.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text026: PracticeText = {
  id: 'it-text-026',
  language: LanguageCode.it,
  content:
    'Il gruppo CQ compare in alcune parole e nelle loro derivate. Si scrivono acqua, acquisto, nacque e tacque.',
};
