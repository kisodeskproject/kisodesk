// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'it-text-039',
  language: LanguageCode.it,
  content:
    'Dà, forma del verbo dare, porta l\'accento per distinguersi dalla preposizione da. La parola da non vuole accento.',
};
