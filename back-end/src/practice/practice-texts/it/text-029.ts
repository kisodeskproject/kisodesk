// texto-029.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'it-text-029',
  language: LanguageCode.it,
  content:
    'L\'apostrofo indica spesso la caduta di una vocale. Si scrive l\'amico, un\'amica, quest\'anno e com\'è.',
};
