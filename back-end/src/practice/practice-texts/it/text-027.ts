// texto-027.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text027: PracticeText = {
  id: 'it-text-027',
  language: LanguageCode.it,
  content:
    'La doppia consonante può cambiare il significato di una parola. Pala e palla, sete e sette, casa e cassa non indicano la stessa cosa.',
};
