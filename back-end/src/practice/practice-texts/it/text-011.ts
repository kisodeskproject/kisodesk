// texto-011.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text011: PracticeText = {
  id: 'it-text-011',
  language: LanguageCode.it,
  content:
    'Il caffè espresso occupa un posto importante nella vita quotidiana italiana. Al bar viene spesso bevuto rapidamente al banco, mentre cappuccino e cornetto sono comuni a colazione.',
};
