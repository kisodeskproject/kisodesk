// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'it-text-033',
  language: LanguageCode.it,
  content:
    'La preposizione semplice si unisce spesso all\'articolo. Da di e il nasce del, da a e la nasce alla, da in e gli nasce negli.',
};
