// texto-052.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text052: PracticeText = {
  id: 'it-text-052',
  language: LanguageCode.it,
  content:
    'L\'Italia è divisa in 20 regioni, 5 delle quali hanno uno statuto speciale. Ogni regione comprende province, città metropolitane e comuni.',
};
