// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'it-text-028',
  language: LanguageCode.it,
  content:
    'L\'accento grafico è obbligatorio nelle parole tronche di più sillabe. Esempi sono città, perché, virtù, caffè e lunedì.',
};
