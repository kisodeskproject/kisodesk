// texto-056.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text056: PracticeText = {
  id: 'it-text-056',
  language: LanguageCode.it,
  content:
    'La liberazione dal nazifascismo viene commemorata il 25 aprile. Questa giornata è una festa nazionale in tutta Italia.',
};
