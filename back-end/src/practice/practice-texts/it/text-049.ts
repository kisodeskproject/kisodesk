// texto-049.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text049: PracticeText = {
  id: 'it-text-049',
  language: LanguageCode.it,
  content:
    'Un cappotto costa 120 euro e viene scontato del 25 per cento. Lo sconto è di 30 euro e il prezzo finale è 90 euro.',
};
