// texto-030.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text030: PracticeText = {
  id: 'it-text-030',
  language: LanguageCode.it,
  content:
    'L\'articolo indeterminativo maschile uno si usa davanti a s impura, z, gn, ps e x. Si dice uno studente, uno zaino e uno psicologo.',
};
