// texto-025.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'it-text-025',
  language: LanguageCode.it,
  content:
    'La lettera Q è quasi sempre seguita da U e da un\'altra vocale. Parole comuni sono quando, questo, quadro e acqua.',
};
