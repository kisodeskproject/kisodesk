// texto-089.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text089: PracticeText = {
  id: 'it-text-089',
  language: LanguageCode.it,
  content:
    'Ennio Morricone compose colonne sonore per centinaia di film. Le sue musiche per il western italiano sono diventate immediatamente riconoscibili.',
};
