// texto-032.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text032: PracticeText = {
  id: 'it-text-032',
  language: LanguageCode.it,
  content:
    'Gli articoli determinativi cambiano secondo genere, numero e suono iniziale. Le forme principali sono il, lo, la, i, gli e le.',
};
