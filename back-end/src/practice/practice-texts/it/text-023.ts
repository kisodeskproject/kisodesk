// texto-023.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text023: PracticeText = {
  id: 'it-text-023',
  language: LanguageCode.it,
  content:
    'Per mantenere il suono duro di C e G davanti a E o I si usa la lettera H. Si scrive quindi che, chi, ghe e ghi.',
};
