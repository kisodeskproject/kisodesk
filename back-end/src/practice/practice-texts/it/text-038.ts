// texto-038.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text038: PracticeText = {
  id: 'it-text-038',
  language: LanguageCode.it,
  content:
    'Sì, avverbio di affermazione, porta l\'accento. Si senza accento è invece un pronome, come nella frase si alza presto.',
};
