// texto-092.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text092: PracticeText = {
  id: 'no-text-092',
  language: LanguageCode.no,
  content:
    'Karpe er en norsk rapduo som kombinerer personlige tekster, samfunnstemaer og store sceneshow.',
};
