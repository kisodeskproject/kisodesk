// texto-026.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text026: PracticeText = {
  id: 'no-text-026',
  language: LanguageCode.no,
  content:
    'Adjektiver bøyes etter kjønn og tall. Man skriver en stor bil, et stort hus og flere store trær.',
};
