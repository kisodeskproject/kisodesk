// texto-037.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'no-text-037',
  language: LanguageCode.no,
  content:
    'I leddsetninger står ikke vanligvis foran det finitte verbet. Man skriver fordi jeg ikke kommer og når hun ikke arbeider.',
};
