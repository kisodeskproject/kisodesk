// texto-085.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text085: PracticeText = {
  id: 'no-text-085',
  language: LanguageCode.no,
  content:
    'Edvard Munch malte Skrik, et av verdens mest gjenkjennelige kunstverk. Bildet finnes i flere versjoner.',
};
