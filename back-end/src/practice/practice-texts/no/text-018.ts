// texto-018.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text018: PracticeText = {
  id: 'no-text-018',
  language: LanguageCode.no,
  content:
    'Norge er kjent for vintersport. Langrenn, skihopping og alpint har en sterk plass i både fritidsliv og internasjonale mesterskap.',
};
