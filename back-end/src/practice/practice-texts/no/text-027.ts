// texto-027.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text027: PracticeText = {
  id: 'no-text-027',
  language: LanguageCode.no,
  content:
    'Verb bøyes ikke etter person på norsk. Man sier jeg skriver, du skriver og de skriver med samme verbform.',
};
