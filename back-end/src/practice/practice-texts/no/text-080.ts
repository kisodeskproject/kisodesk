// texto-080.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text080: PracticeText = {
  id: 'no-text-080',
  language: LanguageCode.no,
  content:
    'Olje og gass ble svært viktig for norsk økonomi fra 1970-årene. Statlige inntekter ble senere plassert i et stort fond for framtidige generasjoner.',
};
