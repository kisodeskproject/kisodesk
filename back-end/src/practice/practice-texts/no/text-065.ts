// texto-065.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text065: PracticeText = {
  id: 'no-text-065',
  language: LanguageCode.no,
  content:
    'Kristningen av Norge skjedde gradvis rundt år 1000. Kongene Olav Tryggvason og Olav Haraldsson spilte viktige roller.',
};
