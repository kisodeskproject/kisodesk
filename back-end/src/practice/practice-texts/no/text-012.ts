// texto-012.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text012: PracticeText = {
  id: 'no-text-012',
  language: LanguageCode.no,
  content:
    'Norge har to offisielle skriftmål: bokmål og nynorsk. I tillegg har samiske språk offisiell status i flere områder.',
};
