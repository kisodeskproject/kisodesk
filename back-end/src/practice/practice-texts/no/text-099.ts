// texto-099.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text099: PracticeText = {
  id: 'no-text-099',
  language: LanguageCode.no,
  content:
    'Magnus Carlsen har vært en av verdens sterkeste sjakkspillere. Suksessen hans har økt interessen for sjakk i Norge.',
};
