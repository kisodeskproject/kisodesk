// texto-023.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text023: PracticeText = {
  id: 'no-text-023',
  language: LanguageCode.no,
  content:
    'Bokmål bruker både en, ei og et som ubestemte artikler. Man kan skrive en bok, ei dør og et hus.',
};
