// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'no-text-028',
  language: LanguageCode.no,
  content:
    'Presens får ofte endingen -r. Eksempler er snakker, leser, skriver og bor.',
};
