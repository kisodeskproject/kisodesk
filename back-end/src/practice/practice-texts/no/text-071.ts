// texto-071.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text071: PracticeText = {
  id: 'no-text-071',
  language: LanguageCode.no,
  content:
    'Danmark-Norge deltok i flere europeiske kriger. Handel, skipsfart og trelast var viktige deler av økonomien.',
};
