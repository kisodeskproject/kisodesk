// texto-017.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text017: PracticeText = {
  id: 'no-text-017',
  language: LanguageCode.no,
  content:
    'Bunaden er en festdrakt med sterke regionale variasjoner. Mønstre, sølv, farger og broderier kan vise til bestemte områder og tradisjoner.',
};
