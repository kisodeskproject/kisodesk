// texto-042.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text042: PracticeText = {
  id: 'no-text-042',
  language: LanguageCode.no,
  content:
    'Fire billetter koster 125 kroner hver. Den totale prisen blir 500 kroner.',
};
