// texto-048.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text048: PracticeText = {
  id: 'no-text-048',
  language: LanguageCode.no,
  content:
    'En vanntank inneholder 80 liter og mister 15 prosent av innholdet. Etterpå er det 68 liter igjen.',
};
