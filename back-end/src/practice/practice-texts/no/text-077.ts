// texto-077.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text077: PracticeText = {
  id: 'no-text-077',
  language: LanguageCode.no,
  content:
    'I 1905 ble unionen med Sverige oppløst på fredelig vis. Prins Carl av Danmark ble valgt til norsk konge og tok navnet Haakon VII.',
};
