// texto-045.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text045: PracticeText = {
  id: 'no-text-045',
  language: LanguageCode.no,
  content:
    'Et norsk postnummer består av fire sifre, for eksempel 0150. Postnummeret står foran poststedet i en adresse.',
};
