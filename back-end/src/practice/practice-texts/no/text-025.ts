// texto-025.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'no-text-025',
  language: LanguageCode.no,
  content:
    'Flertall av substantiv dannes på flere måter. En stol blir stoler, et hus blir hus, og ei bok blir bøker.',
};
