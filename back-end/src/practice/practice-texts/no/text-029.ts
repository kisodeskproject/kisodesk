// texto-029.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'no-text-029',
  language: LanguageCode.no,
  content:
    'Preteritum av svake verb ender ofte på -et, -te, -de eller -dde. Eksempler er kastet, kjøpte, levde og bodde.',
};
