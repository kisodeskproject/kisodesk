// texto-063.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text063: PracticeText = {
  id: 'no-text-063',
  language: LanguageCode.no,
  content:
    'Vikingtiden varte omtrent fra slutten av 700-tallet til midten av 1000-tallet. Nordmenn reiste som handelsfolk, bosettere og krigere.',
};
