// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'no-text-035',
  language: LanguageCode.no,
  content:
    'Ord med dobbel konsonant mister ofte én konsonant foran en ny konsonant. Derfor skriver man trygg, men trygt.',
};
