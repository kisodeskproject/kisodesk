// texto-016.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text016: PracticeText = {
  id: 'no-text-016',
  language: LanguageCode.no,
  content:
    '17. mai er Norges nasjonaldag. Dagen feires med barnetog, flagg, musikk, bunader og store folkemengder i byer og bygder.',
};
