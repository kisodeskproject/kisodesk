// texto-090.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text090: PracticeText = {
  id: 'no-text-090',
  language: LanguageCode.no,
  content:
    'Aurora er en norsk artist kjent for en lys stemme og atmosfærisk popmusikk. Hun har fått et stort internasjonalt publikum.',
};
