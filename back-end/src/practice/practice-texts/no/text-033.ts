// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'no-text-033',
  language: LanguageCode.no,
  content:
    'Sj-lyden kan skrives på flere måter. Den finnes i ord som sjø, skjorte, skje, stasjon og journalist.',
};
