// texto-041.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text041: PracticeText = {
  id: 'no-text-041',
  language: LanguageCode.no,
  content:
    'Toget går klokken 08.35 og kommer fram klokken 11.10. Reisen varer i 2 timer og 35 minutter.',
};
