// texto-011.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text011: PracticeText = {
  id: 'no-text-011',
  language: LanguageCode.no,
  content:
    'Bergen er kjent for mye regn, historiske trehus og fjellene rundt byen. Bryggen minner om byens rolle i hansahandelen.',
};
