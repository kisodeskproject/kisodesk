// texto-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'no-text-024',
  language: LanguageCode.no,
  content:
    'Bestemt form dannes ofte med en ending. En bil blir bilen, ei bok blir boka, og et bord blir bordet.',
};
