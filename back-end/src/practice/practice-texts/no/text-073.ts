// texto-073.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text073: PracticeText = {
  id: 'no-text-073',
  language: LanguageCode.no,
  content:
    'Grunnloven ble vedtatt 17. mai 1814. Norge gikk senere samme år inn i en union med Sverige, men beholdt egne institusjoner.',
};
