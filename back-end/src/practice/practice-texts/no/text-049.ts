// texto-049.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text049: PracticeText = {
  id: 'no-text-049',
  language: LanguageCode.no,
  content:
    'En jakke koster 1200 kroner og selges med 25 prosent rabatt. Rabatten er 300 kroner, og den nye prisen er 900 kroner.',
};
