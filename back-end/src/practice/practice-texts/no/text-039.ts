// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'no-text-039',
  language: LanguageCode.no,
  content:
    'Direkte tale markeres med anførselstegn eller replikkstrek. Tegnsettingen avhenger av hvordan replikken er bygget inn i teksten.',
};
