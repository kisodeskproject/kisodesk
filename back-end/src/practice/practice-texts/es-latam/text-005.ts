import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text005: PracticeText = {
  id: 'es-text-005',
  language: LanguageCode.es,
  content:
    'El lago Titicaca se encuentra entre Perú y Bolivia, a más de tres mil ochocientos metros de altitud. Sus comunidades mantienen tradiciones de navegación, agricultura y tejido vinculadas a una historia de muchos siglos.',
};
