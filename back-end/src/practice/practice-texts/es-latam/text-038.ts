import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text038: PracticeText = {
  id: 'es-text-038',
  language: LanguageCode.es,
  content:
    'Aun se escribe sin tilde cuando equivale a incluso, hasta o también. Aún lleva tilde cuando puede sustituirse por todavía, como en la oración: el paquete aún no ha llegado.',
};
