import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text042: PracticeText = {
  id: 'es-text-042',
  language: LanguageCode.es,
  content:
    'El sistema decimal utiliza 10 cifras: del 0 al 9. Con ellas se representan cantidades pequeñas o enormes, y el valor de cada cifra cambia según la posición que ocupa dentro del número.',
};
