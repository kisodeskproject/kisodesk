import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'es-text-034',
  language: LanguageCode.es,
  content:
    'La r representa un sonido fuerte al inicio de palabra, como en río. Entre vocales, ese sonido fuerte se escribe con rr, como en perro, mientras que una sola r produce un sonido suave, como en pero.',
};
