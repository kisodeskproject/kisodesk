import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text096: PracticeText = {
  id: 'es-text-096',
  language: LanguageCode.es,
  content:
    'El cine latinoamericano ha retratado migraciones, desigualdad, memoria, vida urbana y conflictos familiares. Películas de México, Argentina, Brasil, Chile y Colombia han recibido premios y reconocimiento en festivales internacionales.',
};
