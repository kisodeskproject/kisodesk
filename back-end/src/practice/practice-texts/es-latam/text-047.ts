import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'es-text-047',
  language: LanguageCode.es,
  content:
    'El número 1 000 000 se lee un millón y contiene seis ceros. Diez millones se escriben 10 000 000, mientras que cien millones se representan como 100 000 000.',
};
