import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text046: PracticeText = {
  id: 'es-text-046',
  language: LanguageCode.es,
  content:
    'Una docena contiene 12 unidades y media docena contiene 6. Si se compran 3 docenas de naranjas, el total es de 36 piezas antes de retirar las que estén dañadas.',
};
