import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text051: PracticeText = {
  id: 'es-text-051',
  language: LanguageCode.es,
  content:
    'Un año común tiene 365 días, mientras que un año bisiesto tiene 366. El día adicional se agrega a febrero, que pasa de 28 a 29 días.',
};
