import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text095: PracticeText = {
  id: 'es-text-095',
  language: LanguageCode.es,
  content:
    'El reguetón se desarrolló a partir de intercambios musicales entre Panamá, Puerto Rico, el Caribe y comunidades latinas de Estados Unidos. Con el tiempo se convirtió en uno de los géneros más escuchados internacionalmente.',
};
