import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'es-text-035',
  language: LanguageCode.es,
  content:
    'El punto y coma separa ideas relacionadas que ya contienen comas o que requieren una pausa intermedia. No sustituye siempre al punto, pero ayuda a organizar oraciones extensas con mayor claridad.',
};
