import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text031: PracticeText = {
  id: 'es-text-031',
  language: LanguageCode.es,
  content:
    'La diéresis se coloca sobre la u en las sílabas güe y güi cuando esa vocal debe pronunciarse. Aparece en palabras como pingüino, bilingüe y vergüenza, pero no en guerra o guitarra.',
};
