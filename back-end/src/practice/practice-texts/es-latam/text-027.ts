import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text027: PracticeText = {
  id: 'es-text-027',
  language: LanguageCode.es,
  content:
    'Antes de b y p se escribe m, como en cambio, también, campo y empezar. Esta regla evita combinaciones poco habituales y refleja la manera en que se articulan esos sonidos en español.',
};
