import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text044: PracticeText = {
  id: 'es-text-044',
  language: LanguageCode.es,
  content:
    'Un litro se divide en 1 000 mililitros. Si una botella contiene 750 mililitros, entonces guarda tres cuartas partes de un litro y faltan 250 mililitros para completarlo.',
};
