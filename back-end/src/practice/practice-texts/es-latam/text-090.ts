import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text090: PracticeText = {
  id: 'es-text-090',
  language: LanguageCode.es,
  content:
    'La bossa nova brasileña combinó elementos de samba y jazz con una interpretación suave e íntima. Aunque nació en portugués, su influencia se extendió por toda América Latina y por escenarios internacionales.',
};
