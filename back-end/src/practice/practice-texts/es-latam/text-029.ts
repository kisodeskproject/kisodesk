import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'es-text-029',
  language: LanguageCode.es,
  content:
    'Se escribe b después de m en palabras como ambiente y cambiar. También suelen escribirse con b las terminaciones del pretérito imperfecto de los verbos en ar: cantaba, caminábamos y trabajaban.',
};
