import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text063: PracticeText = {
  id: 'es-text-063',
  language: LanguageCode.es,
  content:
    'El Imperio inca conectó amplios territorios andinos mediante una extensa red de caminos. Los mensajeros recorrían tramos organizados y facilitaban la comunicación entre regiones montañosas separadas por enormes distancias.',
};
