import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text040: PracticeText = {
  id: 'es-text-040',
  language: LanguageCode.es,
  content:
    'Las abreviaturas suelen terminar con punto, como pág. o núm. Las siglas, en cambio, normalmente se escriben sin puntos y con mayúsculas, como ONU, GPS o ADN.',
};
