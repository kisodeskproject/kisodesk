import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text052: PracticeText = {
  id: 'es-text-052',
  language: LanguageCode.es,
  content:
    'La mitad de 240 es 120, la tercera parte es 80 y la cuarta parte es 60. Estas fracciones permiten dividir una misma cantidad en grupos iguales de diferentes tamaños.',
};
