import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text030: PracticeText = {
  id: 'es-text-030',
  language: LanguageCode.es,
  content:
    'Se escribe v después de n en palabras como enviar, invierno e invitación. Aunque b y v suelen pronunciarse de manera semejante, la escritura correcta depende de la forma establecida para cada palabra.',
};
