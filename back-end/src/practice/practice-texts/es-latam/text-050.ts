import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text050: PracticeText = {
  id: 'es-text-050',
  language: LanguageCode.es,
  content:
    'Si una persona lee 18 páginas por día durante 5 días, completa 90 páginas. Para terminar un libro de 150 páginas, todavía tendría que leer 60 páginas adicionales.',
};
