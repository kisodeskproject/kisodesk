import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text032: PracticeText = {
  id: 'es-text-032',
  language: LanguageCode.es,
  content:
    'La letra c puede representar sonidos distintos según la vocal que la siga. En casa y cosa suena como k; delante de e o i se pronuncia como s en gran parte de América Latina.',
};
