import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text012: PracticeText = {
  id: 'es-text-012',
  language: LanguageCode.es,
  content:
    'El salar de Uyuni, en Bolivia, es una inmensa planicie cubierta de sal. Durante la temporada de lluvias, una capa delgada de agua puede convertir su superficie en un espejo natural de gran extensión.',
};
