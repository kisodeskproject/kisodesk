import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text059: PracticeText = {
  id: 'es-text-059',
  language: LanguageCode.es,
  content:
    'El número romano X representa 10, L representa 50 y C representa 100. Así, XXV equivale a 25, mientras que XC representa 90 porque 10 aparece antes de 100.',
};
