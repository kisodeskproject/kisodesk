import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text055: PracticeText = {
  id: 'es-text-055',
  language: LanguageCode.es,
  content:
    'Si una cuenta de 480 pesos se divide entre 6 personas, cada una debe pagar 80 pesos. Si además dejan una propina total de 60 pesos, aportan 10 pesos extra por persona.',
};
