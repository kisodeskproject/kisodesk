import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text004: PracticeText = {
  id: 'es-text-004',
  language: LanguageCode.es,
  content:
    'El desierto de Atacama, situado principalmente en Chile, es uno de los lugares más secos del mundo. Sus cielos despejados y su escasa humedad lo convierten en una zona privilegiada para la observación astronómica.',
};
