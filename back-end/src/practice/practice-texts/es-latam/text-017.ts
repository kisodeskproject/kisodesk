import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text017: PracticeText = {
  id: 'es-text-017',
  language: LanguageCode.es,
  content:
    'Cuba es la isla de mayor tamaño del Caribe. Su música, su arquitectura y su historia han influido ampliamente en la cultura regional, especialmente mediante géneros como el son, la rumba y el bolero.',
};
