import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text056: PracticeText = {
  id: 'es-text-056',
  language: LanguageCode.es,
  content:
    'El promedio de 8, 10 y 12 es 10. Para calcularlo, se suman los tres valores, se obtiene 30 y después se divide el resultado entre la cantidad de datos.',
};
