import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text053: PracticeText = {
  id: 'es-text-053',
  language: LanguageCode.es,
  content:
    'Una velocidad de 60 kilómetros por hora significa recorrer, en promedio, 1 kilómetro por minuto. A ese ritmo, un trayecto de 90 kilómetros tomaría aproximadamente 1 hora y 30 minutos.',
};
