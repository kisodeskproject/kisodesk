import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text088: PracticeText = {
  id: 'es-text-088',
  language: LanguageCode.es,
  content:
    'La cumbia nació en la costa caribeña de Colombia y después adoptó estilos propios en distintos países. Existen variantes mexicanas, argentinas, peruanas, chilenas y centroamericanas con instrumentos y sonidos particulares.',
};
