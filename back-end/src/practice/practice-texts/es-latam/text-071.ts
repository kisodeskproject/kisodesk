import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text071: PracticeText = {
  id: 'es-text-071',
  language: LanguageCode.es,
  content:
    'Simón Bolívar participó en las campañas de independencia de varios territorios del norte de Sudamérica. Su proyecto político buscaba mantener unidas algunas de las nuevas repúblicas, aunque las diferencias regionales dificultaron esa meta.',
};
