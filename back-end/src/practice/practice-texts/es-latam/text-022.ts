import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text022: PracticeText = {
  id: 'es-text-022',
  language: LanguageCode.es,
  content:
    'Las palabras graves o llanas llevan tilde cuando no terminan en vocal, en n ni en s. Árbol, lápiz y difícil son ejemplos claros; casa, joven y lunes no necesitan tilde.',
};
