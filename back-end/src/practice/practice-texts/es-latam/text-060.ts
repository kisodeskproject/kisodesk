import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text060: PracticeText = {
  id: 'es-text-060',
  language: LanguageCode.es,
  content:
    'Si un vehículo consume 8 litros de combustible por cada 100 kilómetros, necesitará aproximadamente 24 litros para recorrer 300 kilómetros en condiciones similares.',
};
