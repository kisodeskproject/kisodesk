import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text026: PracticeText = {
  id: 'es-text-026',
  language: LanguageCode.es,
  content:
    'Después de un punto se escribe mayúscula. También llevan mayúscula inicial los nombres propios de personas, lugares, instituciones y festividades, mientras que los días de la semana y los meses se escriben normalmente con minúscula.',
};
