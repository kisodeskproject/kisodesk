import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text057: PracticeText = {
  id: 'es-text-057',
  language: LanguageCode.es,
  content:
    'Una computadora puede almacenar 1 024 megabytes en un gigabyte según la medida binaria tradicional. En algunos contextos comerciales se usa una equivalencia decimal de 1 000 megabytes por gigabyte.',
};
