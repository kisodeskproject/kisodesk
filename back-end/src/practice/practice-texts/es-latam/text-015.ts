import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text015: PracticeText = {
  id: 'es-text-015',
  language: LanguageCode.es,
  content:
    'El guaraní es idioma oficial de Paraguay junto con el español. Muchas personas usan ambas lenguas en la vida cotidiana, y es común que aparezcan combinadas en conversaciones, canciones y expresiones populares.',
};
