import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'es-text-036',
  language: LanguageCode.es,
  content:
    'La coma se usa para separar elementos de una enumeración, incluir explicaciones breves o marcar ciertos conectores. No debe colocarse entre el sujeto y el verbo cuando no existe una razón sintáctica.',
};
