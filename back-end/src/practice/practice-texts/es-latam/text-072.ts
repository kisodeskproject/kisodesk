import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text072: PracticeText = {
  id: 'es-text-072',
  language: LanguageCode.es,
  content:
    'José de San Martín encabezó campañas militares decisivas en el sur del continente. Su cruce de los Andes permitió avanzar desde el Río de la Plata hacia Chile y después apoyar la independencia del Perú.',
};
