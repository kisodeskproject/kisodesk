import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'es-text-024',
  language: LanguageCode.es,
  content:
    'Los monosílabos generalmente no llevan tilde. Sin embargo, la tilde diacrítica permite distinguir palabras iguales con funciones distintas, como tú y tu, él y el, sí y si, o más y mas.',
};
