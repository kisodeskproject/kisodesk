import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text070: PracticeText = {
  id: 'es-text-070',
  language: LanguageCode.es,
  content:
    'Miguel Hidalgo inició en 1810 una insurrección en la Nueva España que contribuyó al proceso de independencia de México. El movimiento continuó con otros líderes y culminó políticamente en 1821.',
};
