import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text045: PracticeText = {
  id: 'es-text-045',
  language: LanguageCode.es,
  content:
    'El 25 % representa una cuarta parte de una cantidad. Si un producto cuesta 800 pesos y recibe un descuento del 25 %, la rebaja es de 200 pesos y el precio final queda en 600.',
};
