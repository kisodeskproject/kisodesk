import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'es-text-025',
  language: LanguageCode.es,
  content:
    'Los signos de interrogación y exclamación se escriben al inicio y al final de la expresión. Esta característica del español ayuda a reconocer desde el principio la intención de una pregunta o una exclamación.',
};
