import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'es-text-028',
  language: LanguageCode.es,
  content:
    'La letra h no representa un sonido en la pronunciación estándar, pero forma parte de muchas palabras. Se escribe en términos como hacer, hablar, historia y ahora, por lo que conviene aprender cada caso.',
};
