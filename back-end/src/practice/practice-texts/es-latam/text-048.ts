import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text048: PracticeText = {
  id: 'es-text-048',
  language: LanguageCode.es,
  content:
    'Una temperatura de 0 grados Celsius marca el punto de congelación del agua en condiciones normales. A 100 grados Celsius, el agua alcanza su punto de ebullición al nivel del mar.',
};
