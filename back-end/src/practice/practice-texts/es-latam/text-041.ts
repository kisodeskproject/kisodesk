import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text041: PracticeText = {
  id: 'es-text-041',
  language: LanguageCode.es,
  content:
    'Una semana tiene 7 días, un día tiene 24 horas y una hora contiene 60 minutos. Por lo tanto, una semana completa suma 168 horas o 10 080 minutos.',
};
