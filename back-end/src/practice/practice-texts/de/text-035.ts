// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'de-text-035',
  language: LanguageCode.de,
  content:
    'Adjektive werden kleingeschrieben, solange sie nicht als Substantiv verwendet werden. Man schreibt ein schönes Bild, aber etwas Schönes.',
};
