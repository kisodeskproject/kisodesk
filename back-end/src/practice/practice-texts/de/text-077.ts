// texto-077.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text077: PracticeText = {
  id: 'de-text-077',
  language: LanguageCode.de,
  content:
    'Nach 1945 wurde Deutschland in vier Besatzungszonen aufgeteilt. Berlin wurde ebenfalls geteilt, obwohl die Stadt innerhalb der sowjetischen Besatzungszone lag.',
};
