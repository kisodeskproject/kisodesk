// texto-037.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'de-text-037',
  language: LanguageCode.de,
  content:
    'Trennbare Verben werden im Hauptsatz häufig aufgeteilt. Aus anrufen wird zum Beispiel: Ich rufe dich morgen an.',
};
