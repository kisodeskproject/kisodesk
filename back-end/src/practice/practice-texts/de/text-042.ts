// texto-042.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text042: PracticeText = {
  id: 'de-text-042',
  language: LanguageCode.de,
  content:
    'Vier Eintrittskarten kosten jeweils 12,50 Euro. Der Gesamtpreis beträgt 50,00 Euro und wird auf der Rechnung mit zwei Nachkommastellen angegeben.',
};
