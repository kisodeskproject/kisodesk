// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'de-text-039',
  language: LanguageCode.de,
  content:
    'Die Endungen -ig, -lich und -isch bilden häufig Adjektive. Beispiele sind ruhig, freundlich und praktisch.',
};
