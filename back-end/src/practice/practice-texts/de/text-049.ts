// texto-049.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text049: PracticeText = {
  id: 'de-text-049',
  language: LanguageCode.de,
  content:
    'Ein Mantel kostet ursprünglich 120 Euro und wird um 25 Prozent reduziert. Der Rabatt beträgt 30 Euro, sodass der neue Preis 90 Euro ist.',
};
