// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'de-text-034',
  language: LanguageCode.de,
  content:
    'Viele Verben enden im Infinitiv auf -en oder -n. Beim Konjugieren verändert sich die Endung, zum Beispiel: machen, ich mache, du machst, wir machen.',
};
