// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'de-text-028',
  language: LanguageCode.de,
  content:
    'Infinitivgruppen mit um, ohne, statt, anstatt, außer oder als werden mit Komma abgetrennt. Man schreibt: Sie ging, ohne sich zu verabschieden.',
};
