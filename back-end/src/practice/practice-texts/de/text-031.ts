// texto-031.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text031: PracticeText = {
  id: 'de-text-031',
  language: LanguageCode.de,
  content:
    'Ein Doppelvokal kennzeichnet ebenfalls einen langen Vokal. Beispiele dafür sind Saal, Meer, Boot und Seele, wobei diese Schreibweise nur in bestimmten Wörtern vorkommt.',
};
