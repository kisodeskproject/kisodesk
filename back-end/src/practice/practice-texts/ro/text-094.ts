// texto-094.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text094: PracticeText = {
  id: 'ro-text-094',
  language: LanguageCode.ro,
  content:
    'Festivalul Untold se desfășoară la Cluj-Napoca și este cunoscut pentru muzică electronică, pop și spectacole de mari dimensiuni.',
};
