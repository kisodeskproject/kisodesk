// texto-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'ro-text-024',
  language: LanguageCode.ro,
  content:
    'Există și excepții la regula pentru Â și Î. Cuvintele compuse și derivatele pot păstra litera Î, ca în neînțeles și reîncepe.',
};
