// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'ro-text-039',
  language: LanguageCode.ro,
  content:
    'Înainte de dar, însă, ci și iar se pune de obicei virgulă. Înainte de și nu se pune virgulă într-o enumerare simplă.',
};
