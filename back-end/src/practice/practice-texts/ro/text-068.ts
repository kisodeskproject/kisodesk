// texto-068.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text068: PracticeText = {
  id: 'ro-text-068',
  language: LanguageCode.ro,
  content:
    'Moldova s-a organizat ca principat în secolul al XIV-lea. Bogdan I este asociat cu obținerea independenței față de coroana maghiară.',
};
