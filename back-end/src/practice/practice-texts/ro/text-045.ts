// texto-045.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text045: PracticeText = {
  id: 'ro-text-045',
  language: LanguageCode.ro,
  content:
    'Codul poștal românesc are șase cifre. Primele cifre indică zona geografică și unitatea administrativă.',
};
