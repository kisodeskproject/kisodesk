// texto-047.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'ro-text-047',
  language: LanguageCode.ro,
  content:
    'Un elev primește notele 7, 8, 8, 9 și 10. Media aritmetică a celor cinci rezultate este 8,4.',
};
