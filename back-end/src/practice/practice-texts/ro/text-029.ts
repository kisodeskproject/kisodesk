// texto-029.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'ro-text-029',
  language: LanguageCode.ro,
  content:
    'Pluralul substantivelor se formează în mai multe moduri. Un om devine oameni, o fată devine fete, iar un scaun devine scaune.',
};
