// texto-019.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text019: PracticeText = {
  id: 'ro-text-019',
  language: LanguageCode.ro,
  content:
    'Mămăliga se prepară din mălai și apă. Poate fi servită cu brânză, smântână, tocănițe sau alte mâncăruri tradiționale.',
};
