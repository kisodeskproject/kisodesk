// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'ro-text-035',
  language: LanguageCode.ro,
  content:
    'Cuvântul niciun se scrie de regulă într-un singur cuvânt când are valoare de adjectiv pronominal negativ. Se scrie niciun elev și nicio problemă.',
};
