// texto-046.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text046: PracticeText = {
  id: 'ro-text-046',
  language: LanguageCode.ro,
  content:
    'Prefixul telefonic internațional al României este +40. Un număr mobil poate fi scris ca +40 712 345 678.',
};
