// texto-034.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'pt-text-034',
  language: LanguageCode.pt,
  content:
    'A crase ocorre quando a preposição a se encontra com o artigo feminino a ou as. Em Fui à biblioteca, o verbo exige preposição e biblioteca admite artigo. Antes de verbos e palavras masculinas, normalmente não há crase.',
};
