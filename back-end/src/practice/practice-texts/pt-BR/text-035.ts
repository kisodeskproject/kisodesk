// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'pt-text-035',
  language: LanguageCode.pt,
  content:
    'Nomes próprios, países, cidades, instituições e o início de cada frase recebem letra maiúscula. Dias da semana, meses e estações do ano são escritos com minúscula, salvo quando iniciam uma oração.',
};
