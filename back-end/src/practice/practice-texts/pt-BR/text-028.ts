// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'pt-text-028',
  language: LanguageCode.pt,
  content:
    'Alguns acentos diferenciais permanecem. Pôr, verbo, distingue-se de por, preposição; pôde indica passado e se diferencia de pode. No plural, têm e vêm recebem circunflexo para contrastar com tem e vem.',
};
