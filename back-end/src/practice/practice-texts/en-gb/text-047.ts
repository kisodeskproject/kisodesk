// texto-047.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'en-text-047',
  language: LanguageCode.en,
  content:
    'A theatre has 620 seats and sells 558 tickets. The audience fills 90 per cent of the venue. Exactly 62 seats remain empty when the performance begins.',
};
