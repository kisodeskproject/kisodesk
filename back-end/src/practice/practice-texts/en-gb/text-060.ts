// texto-060.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text060: PracticeText = {
  id: 'en-text-060',
  language: LanguageCode.en,
  content:
    'A savings account contains £2,400 and earns 3 per cent interest in one year. Simple interest adds £72, producing a balance of £2,472 before any tax or fees.',
};
