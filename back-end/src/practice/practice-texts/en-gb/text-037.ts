// texto-037.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'en-text-037',
  language: LanguageCode.en,
  content:
    'A colon introduces an explanation, list or example after a complete clause. For instance: Pack three things: a waterproof coat, sturdy boots and a map. The words before the colon should normally stand on their own.',
};
