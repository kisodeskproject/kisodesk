// texto-030.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text030: PracticeText = {
  id: 'en-text-030',
  language: LanguageCode.en,
  content:
    'Single quotation marks are often preferred for initial quotations in British publishing, with double quotation marks used for a quotation inside another. House styles differ, so consistency is more important than one universal rule.',
};
