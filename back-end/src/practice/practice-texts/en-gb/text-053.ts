// texto-053.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text053: PracticeText = {
  id: 'en-text-053',
  language: LanguageCode.en,
  content:
    'A bus leaves at 08:35 and arrives at 10:05. The journey lasts 1 hour and 30 minutes. A return trip of the same length would total 3 hours of travelling time.',
};
