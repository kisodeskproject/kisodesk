// texto-062.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text062: PracticeText = {
  id: 'en-text-062',
  language: LanguageCode.en,
  content:
    'Hadrian\'s Wall was built across northern England in the second century. It marked a frontier of the Roman Empire and included forts, milecastles and watchtowers. Large sections remain visible today.',
};
