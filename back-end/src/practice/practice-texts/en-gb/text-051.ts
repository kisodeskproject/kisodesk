// texto-051.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text051: PracticeText = {
  id: 'en-text-051',
  language: LanguageCode.en,
  content:
    'A water tank holds 1,200 litres and is 65 per cent full. It contains 780 litres, leaving space for another 420 litres before it reaches capacity.',
};
