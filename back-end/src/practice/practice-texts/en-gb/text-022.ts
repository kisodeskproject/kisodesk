// texto-022.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text022: PracticeText = {
  id: 'en-text-022',
  language: LanguageCode.en,
  content:
    'Many British spellings use ise, as in organise, recognise and apologise. Some publishers prefer ize in words of Greek origin, but ise remains common and widely accepted in British writing.',
};
