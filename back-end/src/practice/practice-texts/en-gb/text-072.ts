// texto-072.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text072: PracticeText = {
  id: 'en-text-072',
  language: LanguageCode.en,
  content:
    'The Industrial Revolution transformed Britain from the eighteenth century onwards. New machinery, factories, mines and transport networks increased production but also created dangerous working conditions and rapidly growing cities.',
};
