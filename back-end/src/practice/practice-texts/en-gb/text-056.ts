// texto-056.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text056: PracticeText = {
  id: 'en-text-056',
  language: LanguageCode.en,
  content:
    'A library contains 32,500 books. If 4 per cent are on loan, 1,300 books are currently borrowed and 31,200 remain in the building.',
};
