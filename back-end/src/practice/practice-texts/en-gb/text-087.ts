// texto-087.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text087: PracticeText = {
  id: 'en-text-087',
  language: LanguageCode.en,
  content:
    'Afternoon tea developed as a social custom involving tea, sandwiches, scones and cakes. It is different from high tea, which historically referred to a more substantial evening meal served at a high table.',
};
