// texto-041.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text041: PracticeText = {
  id: 'en-text-041',
  language: LanguageCode.en,
  content:
    'A bookshop receives 240 novels and places them equally on 12 shelves. Each shelf holds 20 books. If three shelves are cleared for a display, 60 novels must be moved elsewhere.',
};
