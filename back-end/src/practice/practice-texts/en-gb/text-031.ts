// texto-031.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text031: PracticeText = {
  id: 'en-text-031',
  language: LanguageCode.en,
  content:
    'In British style, punctuation is often placed inside quotation marks only when it belongs to the quoted words. Publishers follow different conventions, especially in journalism, fiction and academic writing.',
};
