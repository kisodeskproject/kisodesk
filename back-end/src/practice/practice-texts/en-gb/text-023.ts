// texto-023.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text023: PracticeText = {
  id: 'en-text-023',
  language: LanguageCode.en,
  content:
    'British English usually spells centre, theatre and metre with re at the end. Meter is still used for a measuring device, while metre refers to the unit of length or the rhythm of poetry.',
};
