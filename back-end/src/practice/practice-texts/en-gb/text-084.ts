// texto-084.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text084: PracticeText = {
  id: 'en-text-084',
  language: LanguageCode.en,
  content:
    'James Bond began as a fictional spy in novels by Ian Fleming. Film adaptations turned the character into a global icon associated with gadgets, cars, villains, exotic locations and memorable theme songs.',
};
