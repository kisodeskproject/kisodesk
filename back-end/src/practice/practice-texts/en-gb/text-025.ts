// texto-025.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'en-text-025',
  language: LanguageCode.en,
  content:
    'The British spelling defence uses a c, while the related verb defend uses a d. Similar pairs include offence and offend. These changes reflect the different forms of the noun and verb.',
};
