// texto-012.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text012: PracticeText = {
  id: 'en-text-012',
  language: LanguageCode.en,
  content:
    'Scotland contains hundreds of islands, but only a fraction are permanently inhabited. Island communities preserve distinctive accents, music, crafts and traditions shaped by farming, fishing and maritime travel.',
};
