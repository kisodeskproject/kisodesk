// texto-009.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text009: PracticeText = {
  id: 'en-text-009',
  language: LanguageCode.en,
  content:
    'The White Cliffs of Dover face the English Channel and have long served as a symbolic gateway to Britain. Their pale colour comes from chalk containing the remains of microscopic marine organisms.',
};
