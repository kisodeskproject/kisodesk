// texto-083.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text083: PracticeText = {
  id: 'en-text-083',
  language: LanguageCode.en,
  content:
    'Doctor Who first appeared on television in 1963. The programme follows a time-travelling alien known as the Doctor, whose ability to regenerate allows different actors to reinterpret the same character.',
};
