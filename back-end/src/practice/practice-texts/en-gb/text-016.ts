// texto-016.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text016: PracticeText = {
  id: 'en-text-016',
  language: LanguageCode.en,
  content:
    'The Channel Tunnel links England and France beneath the English Channel. Passenger trains, freight trains and vehicle shuttles travel through it, creating a direct rail connection between Britain and continental Europe.',
};
