// texto-059.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text059: PracticeText = {
  id: 'en-text-059',
  language: LanguageCode.en,
  content:
    'A parcel weighs 3.75 kilograms. Adding 250 grams of packaging raises the total to exactly 4 kilograms. The courier charges £6.80 for that weight band.',
};
