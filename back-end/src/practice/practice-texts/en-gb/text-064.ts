// texto-064.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text064: PracticeText = {
  id: 'en-text-064',
  language: LanguageCode.en,
  content:
    'Magna Carta was sealed in 1215 after conflict between King John and leading barons. Although many clauses addressed medieval disputes, the document later became a symbol of limits on political power and protection under law.',
};
