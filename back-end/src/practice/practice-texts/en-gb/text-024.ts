// texto-024.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'en-text-024',
  language: LanguageCode.en,
  content:
    'Words such as travelling, cancelled and labelled usually double the final consonant in British English. This often happens before endings beginning with a vowel, even when the final syllable is not strongly stressed.',
};
