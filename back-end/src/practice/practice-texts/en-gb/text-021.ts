// texto-021.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text021: PracticeText = {
  id: 'en-text-021',
  language: LanguageCode.en,
  content:
    'British English commonly uses spellings such as colour, favour, honour and labour. These words usually contain our where American English often uses or. A document should follow one spelling system consistently.',
};
