// texto-092.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text092: PracticeText = {
  id: 'en-text-092',
  language: LanguageCode.en,
  content:
    'The West End is London\'s main commercial theatre district. Its musicals and plays attract large audiences, while long-running productions can become landmarks of the city\'s cultural life.',
};
