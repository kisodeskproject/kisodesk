// texto-088.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text088: PracticeText = {
  id: 'en-text-088',
  language: LanguageCode.en,
  content:
    'The Premier League attracts football audiences around the world. Clubs combine local rivalries with international players, managers and supporters, while matchdays remain strongly connected to neighbourhood identity.',
};
