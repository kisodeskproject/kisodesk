// texto-029.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'en-text-029',
  language: LanguageCode.en,
  content:
    'British publishers vary in their use of the serial comma. It may be omitted in a simple list, but it is useful when the final items could otherwise be misunderstood or grouped incorrectly.',
};
