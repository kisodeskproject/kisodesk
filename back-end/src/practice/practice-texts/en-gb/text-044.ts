// texto-044.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text044: PracticeText = {
  id: 'en-text-044',
  language: LanguageCode.en,
  content:
    'A walker covers 7.5 kilometres before lunch and 6.8 kilometres afterwards. The full route measures 14.3 kilometres. At an average pace of 5 kilometres per hour, it takes just under three hours.',
};
