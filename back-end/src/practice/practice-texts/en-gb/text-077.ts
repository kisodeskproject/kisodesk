// texto-077.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text077: PracticeText = {
  id: 'en-text-077',
  language: LanguageCode.en,
  content:
    'Women over the age of thirty gained voting rights under certain conditions in 1918. Equal voting rights with men followed in 1928 after decades of campaigning by suffragists and suffragettes.',
};
