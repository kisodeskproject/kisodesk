// texto-049.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text049: PracticeText = {
  id: 'en-text-049',
  language: LanguageCode.en,
  content:
    'A cyclist travels 42 kilometres in two hours and 30 minutes. Converting the time to 2.5 hours gives an average speed of 16.8 kilometres per hour.',
};
