// texto-039.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'en-text-039',
  language: LanguageCode.en,
  content:
    'British English often uses have got to express possession or obligation. \'I have got a bicycle\' and \'We have got to leave\' are natural forms, although simpler alternatives are also correct.',
};
