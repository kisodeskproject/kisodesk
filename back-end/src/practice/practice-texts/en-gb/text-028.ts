// texto-028.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'en-text-028',
  language: LanguageCode.en,
  content:
    'A comma can separate introductory clauses, items in a list and additional information. It should clarify meaning rather than interrupt the sentence unnecessarily. Reading the sentence aloud can help reveal natural pauses.',
};
