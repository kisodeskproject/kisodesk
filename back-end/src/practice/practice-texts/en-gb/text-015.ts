// texto-015.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text015: PracticeText = {
  id: 'en-text-015',
  language: LanguageCode.en,
  content:
    'The United Kingdom has a dense network of public footpaths. Many cross private land under established rights of way, allowing walkers to travel through fields, woods, villages and open countryside.',
};
