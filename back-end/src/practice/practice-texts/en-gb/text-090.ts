// texto-090.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text090: PracticeText = {
  id: 'en-text-090',
  language: LanguageCode.en,
  content:
    'British comedy often uses understatement, awkward situations, wordplay and social embarrassment. Sketch shows, sitcoms, panel programmes and stand-up performances have all contributed to its international reputation.',
};
