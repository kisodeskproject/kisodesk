// texto-027.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text027: PracticeText = {
  id: 'en-text-027',
  language: LanguageCode.en,
  content:
    'A complete sentence normally contains a subject and a finite verb and expresses a complete thought. Fragments may be useful in informal writing, but formal prose usually requires full sentences.',
};
