// texto-032.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text032: PracticeText = {
  id: 'en-text-032',
  language: LanguageCode.en,
  content:
    'Use fewer with items that can be counted and less with uncountable quantities. Write fewer books and fewer mistakes, but less water and less time. Informal speech may not always follow this distinction.',
};
