// texto-013.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text013: PracticeText = {
  id: 'en-text-013',
  language: LanguageCode.en,
  content:
    'The English village of Greenwich gave its name to Greenwich Mean Time. The Prime Meridian passes through the Royal Observatory, providing a reference line for measuring longitude around the world.',
};
