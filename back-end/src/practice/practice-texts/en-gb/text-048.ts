// texto-048.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text048: PracticeText = {
  id: 'en-text-048',
  language: LanguageCode.en,
  content:
    'A school orders 18 boxes containing 24 pencils each. The delivery includes 432 pencils. Shared equally among 12 classes, each class receives 36 pencils.',
};
