// texto-036.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'en-text-036',
  language: LanguageCode.en,
  content:
    'A semicolon can join two closely related independent clauses. It can also separate complex items in a list when those items already contain commas. Both uses help readers follow the structure of a sentence.',
};
