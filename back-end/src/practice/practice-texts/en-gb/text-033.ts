// texto-033.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'en-text-033',
  language: LanguageCode.en,
  content:
    'Their shows possession, there refers to a place or existence, and they\'re means they are. Because the three forms sound alike, checking the role of the word in the sentence is essential.',
};
