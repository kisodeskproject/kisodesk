// texto-018.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text018: PracticeText = {
  id: 'en-text-018',
  language: LanguageCode.en,
  content:
    'The British coastline is highly varied, with cliffs, beaches, estuaries, salt marshes and rocky coves. No place in the United Kingdom is extremely far from the sea, which has strongly influenced food, trade and settlement.',
};
