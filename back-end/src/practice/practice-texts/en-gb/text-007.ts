// texto-007.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text007: PracticeText = {
  id: 'en-text-007',
  language: LanguageCode.en,
  content:
    'Wales has more castles per square mile than many other parts of Europe. Some were built by Welsh princes, while others were constructed by English kings. Their ruins record centuries of conflict, defence and political change.',
};
