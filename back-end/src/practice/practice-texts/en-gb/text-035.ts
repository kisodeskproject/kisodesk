// texto-035.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'en-text-035',
  language: LanguageCode.en,
  content:
    'Its is possessive, while it\'s means it is or it has. Write \'The company changed its policy\' and \'It\'s been a busy week\'. The possessive form has no apostrophe.',
};
