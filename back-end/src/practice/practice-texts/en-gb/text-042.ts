// texto-042.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text042: PracticeText = {
  id: 'en-text-042',
  language: LanguageCode.en,
  content:
    'A train ticket costs £48 and a railcard reduces the price by 25 per cent. The discount is £12, so the passenger pays £36 for the journey.',
};
