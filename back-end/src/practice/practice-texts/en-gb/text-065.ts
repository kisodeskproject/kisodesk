// texto-065.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text065: PracticeText = {
  id: 'en-text-065',
  language: LanguageCode.en,
  content:
    'The Black Death reached Britain in 1348 and killed a large part of the population. Labour shortages altered wages, farming and social relationships, while repeated outbreaks continued for centuries.',
};
