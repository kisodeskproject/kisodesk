// texto-046.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text046: PracticeText = {
  id: 'en-text-046',
  language: LanguageCode.en,
  content:
    'A recipe requires 750 grams of flour for three loaves. Each loaf therefore uses 250 grams. To bake eight loaves, the baker needs 2,000 grams, or 2 kilograms, of flour.',
};
