// texto-089.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text089: PracticeText = {
  id: 'en-text-089',
  language: LanguageCode.en,
  content:
    'Glastonbury Festival brings together music, performance, visual art and political causes on a large rural site in Somerset. Muddy fields, decorated stages and varied line-ups have become part of its public image.',
};
