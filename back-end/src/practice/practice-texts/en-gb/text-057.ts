// texto-057.ts
import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text057: PracticeText = {
  id: 'en-text-057',
  language: LanguageCode.en,
  content:
    'A family drives 360 miles and the car averages 45 miles per gallon. The trip uses 8 gallons of fuel. At £1.52 per litre, the exact cost would require converting gallons into litres.',
};
