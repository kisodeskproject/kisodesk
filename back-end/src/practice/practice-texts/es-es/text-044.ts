import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text044: PracticeText = {
  id: 'es-text-044',
  language: LanguageCode.es,
  content:
    'Un litro contiene 1.000 mililitros. Si una botella guarda 750 mililitros, tiene tres cuartas partes de un litro y necesita 250 mililitros más para llenarse por completo.',
};
