import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text040: PracticeText = {
  id: 'es-text-040',
  language: LanguageCode.es,
  content:
    'Las abreviaturas suelen terminar en punto, como pág., núm. o aprox. Las siglas se escriben normalmente sin puntos y con mayúsculas, como UE, DNI, AVE o RTVE.',
};
