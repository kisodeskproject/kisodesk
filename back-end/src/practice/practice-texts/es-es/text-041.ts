import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text041: PracticeText = {
  id: 'es-text-041',
  language: LanguageCode.es,
  content:
    'Una semana tiene 7 días, cada día contiene 24 horas y una hora suma 60 minutos. Por tanto, una semana completa equivale a 168 horas o a 10.080 minutos.',
};
