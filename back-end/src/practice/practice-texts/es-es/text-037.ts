import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text037: PracticeText = {
  id: 'es-text-037',
  language: LanguageCode.es,
  content:
    'Porque introduce una causa; por qué aparece en preguntas; porqué funciona como sustantivo, y por que corresponde a la combinación de una preposición con un pronombre o una conjunción.',
};
