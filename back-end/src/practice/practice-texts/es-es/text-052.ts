import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text052: PracticeText = {
  id: 'es-text-052',
  language: LanguageCode.es,
  content:
    'Un tren recorre 300 kilómetros en 2 horas y media. Su velocidad media es de 120 kilómetros por hora, aunque durante el trayecto puede circular a velocidades diferentes.',
};
