import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text031: PracticeText = {
  id: 'es-text-031',
  language: LanguageCode.es,
  content:
    'La diéresis aparece sobre la u en las secuencias güe y güi cuando esa vocal debe pronunciarse. Se usa en pingüino, bilingüe y vergüenza, pero no en guerra, guitarra o conseguir.',
};
