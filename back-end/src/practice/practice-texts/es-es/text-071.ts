import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text071: PracticeText = {
  id: 'es-text-071',
  language: LanguageCode.es,
  content:
    'La guerra de Sucesión española comenzó en 1701 tras la muerte de Carlos II sin descendencia. El conflicto terminó con Felipe V en el trono y con importantes cambios políticos y territoriales.',
};
