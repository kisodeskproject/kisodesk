import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text053: PracticeText = {
  id: 'es-text-053',
  language: LanguageCode.es,
  content:
    'Una receta para 4 personas utiliza 300 gramos de arroz. Para preparar la misma proporción para 10 personas se necesitan 750 gramos, porque la cantidad original se multiplica por 2,5.',
};
