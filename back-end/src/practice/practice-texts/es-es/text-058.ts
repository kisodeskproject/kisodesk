import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text058: PracticeText = {
  id: 'es-text-058',
  language: LanguageCode.es,
  content:
    'En informática, 1.024 megabytes equivalen tradicionalmente a un gigabyte en la medida binaria. En muchos productos comerciales se utiliza la equivalencia decimal de 1.000 megabytes por gigabyte.',
};
