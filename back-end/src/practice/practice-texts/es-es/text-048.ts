import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text048: PracticeText = {
  id: 'es-text-048',
  language: LanguageCode.es,
  content:
    'Un año común tiene 365 días y un año bisiesto tiene 366. El día adicional se añade a febrero, que pasa de 28 a 29 días.',
};
