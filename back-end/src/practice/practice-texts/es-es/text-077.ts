import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text077: PracticeText = {
  id: 'es-text-077',
  language: LanguageCode.es,
  content:
    'La Guerra Civil española se desarrolló entre 1936 y 1939. El conflicto provocó numerosas muertes, destrucción, exilio y una profunda división social que continuó durante la dictadura posterior.',
};
