import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text063: PracticeText = {
  id: 'es-text-063',
  language: LanguageCode.es,
  content:
    'Tras la caída del Imperio romano de Occidente, los visigodos establecieron un reino con capital en Toledo. Su dominio se mantuvo hasta comienzos del siglo VIII, cuando se produjo la conquista musulmana.',
};
