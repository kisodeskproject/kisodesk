import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text054: PracticeText = {
  id: 'es-text-054',
  language: LanguageCode.es,
  content:
    'El 15 % de 200 es 30. Para calcularlo, se multiplica 200 por 15 y se divide el resultado entre 100; por tanto, el 85 % restante es 170.',
};
