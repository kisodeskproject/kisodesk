import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text047: PracticeText = {
  id: 'es-text-047',
  language: LanguageCode.es,
  content:
    'El número 1.000.000 se lee un millón y contiene seis ceros. Si se divide entre 1.000, el resultado es 1.000; si se divide entre 100, el resultado es 10.000.',
};
