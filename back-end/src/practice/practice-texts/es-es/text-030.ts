import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text030: PracticeText = {
  id: 'es-text-030',
  language: LanguageCode.es,
  content:
    'Después de n se escribe v en palabras como enviar, invierno, inventar y convivencia. Aunque b y v suelen pronunciarse igual, su escritura debe respetar la forma fijada para cada término.',
};
