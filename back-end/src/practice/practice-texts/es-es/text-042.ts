import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text042: PracticeText = {
  id: 'es-text-042',
  language: LanguageCode.es,
  content:
    'El sistema decimal utiliza 10 cifras, desde el 0 hasta el 9. El valor de cada cifra depende de su posición: en 5.432, el 5 representa cinco millares y el 4 representa cuatro centenas.',
};
