import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text013: PracticeText = {
  id: 'es-text-013',
  language: LanguageCode.es,
  content:
    'En algunas zonas de Galicia existen hórreos, construcciones elevadas destinadas tradicionalmente a guardar grano y protegerlo de la humedad y los animales. Sus formas varían según la comarca y los materiales disponibles.',
};
