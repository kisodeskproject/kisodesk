import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text026: PracticeText = {
  id: 'es-text-026',
  language: LanguageCode.es,
  content:
    'Los nombres propios de personas, lugares e instituciones comienzan con mayúscula. En cambio, los días de la semana, los meses y las estaciones del año se escriben normalmente con minúscula.',
};
