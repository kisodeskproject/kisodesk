import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text024: PracticeText = {
  id: 'es-text-024',
  language: LanguageCode.es,
  content:
    'Los monosílabos suelen escribirse sin tilde. La tilde diacrítica permite distinguir formas iguales con funciones distintas, como tú y tu, él y el, sí y si, o té y te.',
};
