import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text049: PracticeText = {
  id: 'es-text-049',
  language: LanguageCode.es,
  content:
    'Una temperatura de 0 grados centígrados corresponde al punto de congelación del agua en condiciones normales. A 100 grados, el agua hierve aproximadamente al nivel del mar.',
};
