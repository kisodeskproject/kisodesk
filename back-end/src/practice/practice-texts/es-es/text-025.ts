import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text025: PracticeText = {
  id: 'es-text-025',
  language: LanguageCode.es,
  content:
    'Los signos de interrogación y exclamación se escriben tanto al principio como al final. Gracias a ¿ y ¡, el lector reconoce desde el comienzo si una frase es una pregunta o una exclamación.',
};
