import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text056: PracticeText = {
  id: 'es-text-056',
  language: LanguageCode.es,
  content:
    'Seis amigos comparten una cuenta de 84 euros. Si pagan a partes iguales, cada uno aporta 14 euros; si dejan además 12 euros de propina, cada persona añade 2 euros.',
};
