import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text045: PracticeText = {
  id: 'es-text-045',
  language: LanguageCode.es,
  content:
    'Un descuento del 25 % representa la cuarta parte del precio. Si una chaqueta cuesta 80 euros, la rebaja es de 20 euros y el precio final queda en 60 euros.',
};
