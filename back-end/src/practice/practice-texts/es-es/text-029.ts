import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text029: PracticeText = {
  id: 'es-text-029',
  language: LanguageCode.es,
  content:
    'Las terminaciones del pretérito imperfecto de los verbos acabados en ar se escriben con b: cantaba, viajábamos y estudiaban. También se escriben con b palabras como amable, posibilidad y biblioteca.',
};
