import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text074: PracticeText = {
  id: 'es-text-074',
  language: LanguageCode.es,
  content:
    'A lo largo del siglo XIX, España vivió cambios de régimen, guerras civiles, pronunciamientos militares y procesos de industrialización. Las diferencias regionales y sociales influyeron de forma decisiva en esta etapa.',
};
