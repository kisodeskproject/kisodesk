import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text028: PracticeText = {
  id: 'es-text-028',
  language: LanguageCode.es,
  content:
    'La letra h no representa un sonido en la pronunciación habitual, pero forma parte de numerosas palabras. Se escribe en hacer, hablar, ahora, vehículo y almohada, por lo que conviene aprender cada caso.',
};
