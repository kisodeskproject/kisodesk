import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text034: PracticeText = {
  id: 'es-text-034',
  language: LanguageCode.es,
  content:
    'La r suena fuerte al principio de palabra, como en río. Entre vocales, ese sonido se representa con rr, como en perro; una sola r produce un sonido más suave, como en pero.',
};
