import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text090: PracticeText = {
  id: 'es-text-090',
  language: LanguageCode.es,
  content:
    'La jota se interpreta en distintas regiones españolas y presenta variantes locales. Suele combinar canto, baile e instrumentos como la guitarra, la bandurria, el laúd, la dulzaina o las castañuelas.',
};
