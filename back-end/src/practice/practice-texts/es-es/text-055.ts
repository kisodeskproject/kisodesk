import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text055: PracticeText = {
  id: 'es-text-055',
  language: LanguageCode.es,
  content:
    'Un libro tiene 480 páginas. Si una persona lee 30 páginas al día, necesitará 16 días para terminarlo, siempre que mantenga el mismo ritmo diario.',
};
