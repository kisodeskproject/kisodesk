import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text027: PracticeText = {
  id: 'es-text-027',
  language: LanguageCode.es,
  content:
    'Antes de b y p se escribe m, como en cambio, sombra, campo y empezar. Esta regla refleja la articulación de esos sonidos y evita combinaciones como nb o np dentro de una palabra.',
};
