import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text035: PracticeText = {
  id: 'es-text-035',
  language: LanguageCode.es,
  content:
    'La coma separa elementos de una enumeración, incisos y ciertos conectores. No debe escribirse entre el sujeto y el verbo cuando no existe un inciso que justifique esa pausa.',
};
