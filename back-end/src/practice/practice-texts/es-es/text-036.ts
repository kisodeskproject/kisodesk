import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text036: PracticeText = {
  id: 'es-text-036',
  language: LanguageCode.es,
  content:
    'El punto y coma marca una pausa mayor que la coma y menor que el punto. Resulta útil para separar oraciones relacionadas o elementos complejos que ya contienen comas.',
};
