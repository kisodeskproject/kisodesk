import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text046: PracticeText = {
  id: 'es-text-046',
  language: LanguageCode.es,
  content:
    'Una docena contiene 12 unidades y media docena contiene 6. Si una panadería prepara 4 docenas de bollos, habrá elaborado 48 piezas antes de venderlas.',
};
