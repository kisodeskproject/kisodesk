import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text039: PracticeText = {
  id: 'es-text-039',
  language: LanguageCode.es,
  content:
    'Los prefijos se escriben unidos a una sola palabra, como expresidente, antivirus y posguerra. Se separan cuando afectan a varias palabras y pueden llevar guion delante de siglas o nombres propios.',
};
