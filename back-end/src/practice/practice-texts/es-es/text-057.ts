import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text057: PracticeText = {
  id: 'es-text-057',
  language: LanguageCode.es,
  content:
    'La media de 8, 10 y 12 es 10. Para obtenerla se suman los tres valores, se consigue 30 y se divide entre la cantidad de datos, que es 3.',
};
