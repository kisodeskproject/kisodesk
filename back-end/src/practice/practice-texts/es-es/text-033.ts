import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text033: PracticeText = {
  id: 'es-text-033',
  language: LanguageCode.es,
  content:
    'La g tiene sonido suave en gato, goma y gusano. Delante de e o i puede sonar como j; para conservar el sonido suave se escriben gue y gui, como en guerra y guitarra.',
};
