import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text051: PracticeText = {
  id: 'es-text-051',
  language: LanguageCode.es,
  content:
    'Una hora y media equivale a 90 minutos. Si una película empieza a las 18:20 y dura ese tiempo, terminará a las 19:50, sin contar anuncios ni pausas.',
};
