import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text032: PracticeText = {
  id: 'es-text-032',
  language: LanguageCode.es,
  content:
    'En gran parte de España, la c delante de e o i y la z se pronuncian de forma distinta a la s. Esta diferencia se llama distinción, aunque la ortografía es la misma en todos los territorios hispanohablantes.',
};
