import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text087: PracticeText = {
  id: 'es-text-087',
  language: LanguageCode.es,
  content:
    'La música de Mecano se convirtió en una referencia del pop español de los años ochenta y noventa. Sus canciones combinan melodías electrónicas, historias urbanas y letras que siguen sonando en radio, fiestas y musicales.',
};
