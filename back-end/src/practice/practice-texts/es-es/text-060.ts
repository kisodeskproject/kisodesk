import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text060: PracticeText = {
  id: 'es-text-060',
  language: LanguageCode.es,
  content:
    'Si un coche consume 6 litros por cada 100 kilómetros, necesitará unos 18 litros para recorrer 300 kilómetros. Con un precio de 1,60 euros por litro, el combustible costará 28,80 euros.',
};
