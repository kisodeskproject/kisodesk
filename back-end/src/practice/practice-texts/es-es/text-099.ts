import { LanguageCode } from '@prisma/client';

import type { PracticeText } from '../types';

export const text099: PracticeText = {
  id: 'es-text-099',
  language: LanguageCode.es,
  content:
    'La gastronomía ocupa un lugar destacado en la cultura popular española. Programas de cocina, concursos, redes sociales y rutas gastronómicas difunden recetas como la tortilla, el gazpacho, la paella o las croquetas.',
};
