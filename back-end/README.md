# Backend

NestJS API with Prisma. Copy `.env.example` to `.env`, set the required secrets, run `npm ci`, then use `npm run build`, `npm test`, or `npm run start:dev`.

`docker-compose.example.yml` and the observability examples are local development templates only.
